## 完成進度

- 將專案根目錄的本機參考資料夾由 `resource/` 更名為 `res/`，並同步更新 `.gitignore`、專案筆記與 theme 來源註釋；Demo、build 與正式 source 仍不依賴此資料夾。
- Vue source-mode Demo 改為參考 `res/man020010` 的 SystemJS runtime SFC 載入流程：`vue2-grid.html` 於瀏覽器執行期間動態載入 `js/vue2-grid.vue`。
- 移除 `demo/vue2-grid-app.html`、Vite production 入口設定與未再使用的 `vue2-sfc-entry.js`；SystemJS Vue Demo 入口已由 `demo/vue2-sfc-dev.html` 更名為 `demo/vue2-grid.html`。
- 將 SystemJS 0.21.6 runtime 複製為 `demo/js/system.js`，Vue Demo 改為直接引用此本機檔案，不再從 `node_modules/systemjs/dist` 載入 runtime。
- 將 Vue SFC 由 `demo/vue2-grid.vue` 移至 `demo/js/vue2-grid.vue`，同步更新 SystemJS URL、模組相對路徑、build helper 與文件引用。
- 將 `demo/js/vue2-grid.js` 的 Demo options、locale、資料處理與 helper 全部移入 `demo/js/vue2-grid.vue`，改為直接在 `export default {}` 定義 Vue Options API；移除舊 JS、IIFE、全域 factory 與 side-effect import，並保留 legacy SFC loader template 錨點。
- Vue Demo 改由 `demo/vue2-grid.html` 依序載入 `dist/wrapper/vue.min.js`、`dist/fabui.min.js` 與 `dist/wrapper/fabgrid-vue.min.js` browser global；`demo/js/vue2-grid.vue` 使用 `window.fabuiVue`，不再匯入 Vue、`src/fabui.js` 或 wrapper ESM。
- Vue wrapper build 新增 `dist/wrapper/fabgrid-vue.js` 未壓縮 browser-global 輸出，並同步複製 `vue.min.js`；smoke output 清單同步驗證兩個 wrapper 版本與 Vue runtime。
- 完整 build 移除已淘汰的 Vue production Demo build 步驟，並刪除仍依賴已移除 `demo/js/vue2-sfc-entry.js` 的 `build/build-vue-demo.cjs`。
- Vue Demo 依賴清理只保留 `demo/vue2-grid.html` 與其 runtime 依賴；刪除舊 `demo/build/`、`dist-vue-demo/` 產物及對應 ignore 規則，並移除不再使用的 Vue template compiler 開發依賴。
- FabGrid `editor: color` popup 左側預設色盤依參考 `palette-grid` HTML 的 `aria-label` 精確校正為 10 欄 × 6 列共 60 色；右側 HSV／透明度控制與自訂 palette 行為保持不變。
- `demo/vue2-grid.html` 的 Vue SFC Demo 補齊至 `demo/dev.html` 同等功能：一致的 22 欄順序、Color／DateBox／NumberBox／ComboBox／參照 editor、16 組 theme、status 樣式、group header、filter、remote、Popup Grid、匯出狀態、三語 locale 與 runtime stats。
- 將 Vue 根實例的 `new Vue(...).$mount('#app')` 從 `demo/vue2-grid.html` 移入 `demo/js/vue2-grid.vue`；HTML 入口只保留依賴載入、`System.import()` 與錯誤顯示。
- 使用專案現有 Prettier 統一整理 `demo/js/vue2-grid.vue` 的 template、JavaScript 與 CSS，採兩格空白、100 字元換行及清楚的 template 父子標籤排版。
- 移除 `demo/js/vue2-grid.vue` 多餘的 `window.fabuiVue` 區域變數與 `components.FabGrid` 區域註冊，改用 wrapper browser bundle 已透過 `Vue.use(plugin)` 建立的全域元件註冊。
- 將 `demo/js/vue2-grid.vue` 改成與 `res/man020010/app.vue` 相同的直接 `new Vue({...})` 模式：legacy loader 將 SFC template 自動注入根實例 options，不再使用 `sfcRuntime`、顯式 `template` 或外層 render wrapper。
- 移除未實際影響畫面的 `alternatingRowBackground` 公開 option、accessor、CSS variable、API 文件與 Demo 設定；`alternatingRows` 及各 theme 原有交錯列背景保持不變，smoke 改為直接驗證 computed background。
- 移除 standalone `YymmBox` 定義與 `src/yymmbox` 原始碼、TODO、build guard 及殘留 smoke 區塊；FabGrid 年月功能繼續由 `datebox` 的年份／月份模式提供，內部 helper 統一改用 `YearMonth` 命名。
- 移除 `demo/js/demo.js` 的 `demoHeaderKey` Demo 別名，欄位排序、多語系標題與列群組統一使用實際 `binding`；同步補齊 `yymm` 的三語欄位標題。
- 整理 `demo/js/demo.js`：移除未使用的 row count 常數與空 theme loader，欄位依顯示順序直接定義並刪除二次排序，避免 popup 區域變數遮蔽，且將 Demo 啟動點統一放到檔案末端。
- 補齊 `demo/js/demo.js` Popup Grid 的三語 locale：開啟圖示提示、標題、頁碼、筆數、關閉／清除／取消／確定按鈕與 8 個欄位標題皆隨 Demo 語言切換，已開啟的 Popup 也會即時更新。
- 將 `demo/js/vue2-grid.vue` 功能對齊 `demo/js/demo.js`：主欄位與群組統一使用實際 binding、修正群組標題、統一本機／遠端篩選與 `searchText` 設定、補上 `searchCleared`、工具列 busy 狀態、篩選焦點、Popup 顯示／焦點及統計格式；實際 `name` header key 使用 computed property，避免舊 SystemJS Vue loader 誤判為 component `name`。
- 將 Vue 2 Demo SFC 由 `demo/js/vue2-grid.vue` 重新命名為 `demo/js/demo-vue2.vue`，同步更新 SystemJS 入口、專案規則與 Vue API 文件引用。
- 將 Vue 2 Demo HTML 入口由 `demo/vue2-grid.html` 重新命名為 `demo/demo-vue2.html`，同步更新專案規則與 Vue API 文件網址。
- 將原生與 Vue 2 Demo 重複的 Popup Grid 固定資料集中至 `demo/js/demo-data.js`，兩份 Demo 共用同一份 `lookupRows`；Vue template、columns、loader 與 lifecycle 保持完整。
- 新增 `demo/js/demo-query.js` 共用遠端篩選、條件比對與排序；Pure JS 與 Vue 2 Demo 各自保留完整的 loader、分頁、延遲及 Promise 回傳流程。
- 新增 `demo/js/demo-locales.js` 統一 Pure JS 與 Vue 2 Demo 的三語 toolbar、主欄位、Popup、workflow 與 validation 文字；Vue 保留 locale computed、watcher、template 綁定與欄位 factory。
- 完成 jQuery wrapper 現行契約檢查：補齊 69 個 core／相容事件轉接、native emitter fallback、既有 instance callback 更新／移除及正式 option setter 對應；同步補強 wrapper 測試、API 文件與兩個 jQuery Demo 的共用依賴。
- 將 jQuery build-mode Demo 入口由 `demo/jquery-grid.html` 重新命名為 `demo/demo-jquery.html`，同步更新 README 與專案規則引用。
- `demo/js/demo.js` 新增 Grid 全螢幕切換按鈕，支援進入／離開狀態、三語提示、不支援時停用，以及全螢幕尺寸變更後重新整理 Grid；Pure JS 與 jQuery Demo 共用此功能。
- Vue 2 Demo 補上相同的 Grid 全螢幕切換按鈕，沿用共用三語文字與樣式，並於全螢幕狀態改變後重新整理 wrapper 底層 Grid。
- 將 Pure JS、Vue 2 與 jQuery Demo 重複的完整上方工具列抽至 `demo/js/demo-toolbar.js`；共用品牌、控制項、選項、匯出與全螢幕按鈕 markup，Vue 透過事件橋接與 update API 保留自身狀態管理。
- 新增 `demo/js/demo-jquery.js` 專用 adapter，jQuery Demo 的 Grid 初始化、方法呼叫與事件綁定改為明確使用 `$.fn.fabgrid` 與 jQuery events；共用 `demo.js` 不再自行偵測 jQuery wrapper 後直接操作 core instance。
- README 與 AGENTS 補上 jQuery Demo 分層契約：`demo-jquery.js` 僅負責 wrapper adapter、`demo.js` 保留共用流程，所有狀態變更操作必須經過 `$host.fabgrid(...)`，並保留 `data-demo-adapter="jquery"` 驗證標記。
- 本輪 Demo 重構完成後，Prettier、JavaScript 語法、`git diff --check`、42 項單元測試與完整 build／smoke 均通過；最新一次文件調整未重新 build。
