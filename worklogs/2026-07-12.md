# 2026-07-12 工作紀錄

## 完成進度

- 將安全 binding 存取、prototype pollution 防護、typed comparison 與 dictionary 建立抽離至 `src/core/data-utils.js`。
- 新增純 Node 單元測試，涵蓋 nested binding、危險 binding、特殊群組 key 與不同資料型別比較。
- 新增 `npm test`，提供不需 build、不改寫 `dist` 的快速驗證入口。
- 使用 esbuild JavaScript API 取代正規表示式 JavaScript minifier，保留既有 browser global、ES module、locale 與六個發布主檔契約。
- 整理 `AGENTS.md`，明確區分現行已交付能力與歷史 V1 規劃。
- 更新 source-mode demo 的入口 query version，避免瀏覽器沿用舊模組快取。
- 抽離 `grouping-utils`，集中 group config、state key、custom key 與安全 bucket 建立。
- 抽離 `remote-utils`，集中 pagination clamp、EasyUI sort 參數、remote response 與 GET／POST request descriptor。
- 抽離 `export-utils`，集中 CSV escape、Excel column name、XML escape 與 Excel color 轉換。
- 抽離 `mask-utils` 與 `validation-utils`，集中 grid editor mask、caret、copy 與 validation result 規則。
- 純 Node 單元測試目前為 22 項，涵蓋上述所有新模組、aggregate、ZIP、XLSX package、Excel cell 與 style helpers。
- 依 Grid 領域重新整併模組：資料、remote、pagination、grouping 與 aggregate 集中至 `src/grid/fabgrid-data.js`。
- mask 與 validation helpers 集中至 `src/grid/fabgrid-editor.js`，匯出共用 helper 集中至 `src/grid/fabgrid-export.js`。
- 將只服務 FabGrid 版面的 `fabgrid.css` 移至 `src/grid/`；共用 icon 樣式更名為 `src/fabui.icon.css`，並同步修正公開 CSS entry。
- 將 `getCsv`、`exportCsv`、`getExcelBlob`、`getExcelFrozenColumnCount`、`exportExcel` 與 Blob 下載流程移入 `src/grid/fabgrid-export.js` installer，公開 API 維持不變。
- 將 ZIP、UTF-8 byte、CRC32 writer 移入 `src/grid/fabgrid-export.js`，Excel Blob 不再由主檔注入 ZIP 實作。
- 將 Excel cell XML、style 正規化、style merge 與 alignment helper 移入 `src/grid/fabgrid-export.js`。
- 將 XLSX package manifest、workbook、relationships 與 document properties 產生流程移入 `src/grid/fabgrid-export.js`。
- 完成 worksheet、style registry、group/footer row、frozen pane、column width 與 computed CSS style resolver 搬移，並刪除 `fabgrid.js` 內舊 XLSX／ZIP helper。
- 將 FabGrid 核心、editor、popup 與 loading panel 字型統一為 `helvetica, tahoma, verdana, sans-serif`。
- 將 itemsSource、remote load/reload、GET／POST／Promise loader、loading state、pager options 與 observed source prototype 流程移入 `src/grid/fabgrid-data.js` installer。
- 將 filter、global/column search、sorting、`applyView()` 與 pagination normalization 移入 `src/grid/fabgrid-data.js`。
- 暫停模組拆分，將 grouping/data 尾段、完整 editor 模組與後續重新評估範圍記錄於 `TODO.md`。
- Excel 匯出新增真正 BIFF8 `.xls` 支援；`.xlsx` 維持原自製匯出器，`.xls` 使用 SheetJS 官方 0.20.3 browser bundle。
- Demo toolbar 新增獨立 BIFF8 `.xls` 匯出範例按鈕，並補齊英文、繁中、簡中提示文字。
- BIFF8 `.xls` 補上 number format、hidden columns、column width、autoFilter、group aggregate 與 footer；文件註明 CE 不支援完整視覺 cell style。
- 依需求完整移除 BIFF8 `.xls` 功能、SheetJS 依賴、Demo、測試與文件，Excel 匯出恢復為只支援 `.xlsx`。
- 修正 export 模組重整時遺漏的 `downloadBlob()`，避免 Demo 點擊 XLSX／CSV 匯出時發生 `ReferenceError`。
- 新增 `fabui.Chart` 主 bundle 元件，使用 pure JavaScript SVG 支援直條圖、橫條圖、折線圖與圓餅圖，包含 tooltip、legend、resize、三語文字、Demo、API 與測試。
- 依 Wijmo FlexChart／FlexPie 規格重整 Chart API：加入 `itemsSource`、`bindingX`、`series.binding`、`bindingName`、`binding`、`chartType`、header/footer、axis、legend、tooltip、palette、selection 與 rendering/rendered/selectionChanged 事件，並保留舊 API 相容層。
- 新增 `demo/grid-chart.html` source-mode 整合範例，FabGrid 與 Chart 共用同一個 `itemsSource` 陣列，Grid 編輯完成後即時刷新 Chart，並可切換直條圖、橫條圖與折線圖。
- Chart 新增綁定資料變動偵測與自動 refresh、首次顯示及 refresh 動畫、`selectPoint()` 資料點效果；整合 Demo 完成 Grid／Chart 雙向選取連動。
- 整合 Demo 加入共用資料來源的 Pie 訂單占比範例，支援自動更新動畫、Grid 選取高亮 Pie slice，以及點擊 Pie 反向選取 Grid row。
- Pie 選取資料時會將對應 slice 的中心旋轉至 12 點方向，並沿上方移出圓心，Grid row 選取與直接點擊 Pie 共用相同行為。
- Grid + Chart 整合 Demo 加入獨立 Line Chart，與 Grid、Cartesian Chart、Pie 共用同一份資料來源及雙向選取連動。
- Grid、Column/Bar Chart、Line Chart、Pie Chart 在桌面版整合 Demo 採 2 x 2 排列，小螢幕改為單欄。
- 修正 Pie 初始化尚未選取資料時將 `null` 誤判為 point index，導致旋轉角度與 SVG path 產生 `NaN`、Pie 不可見的問題。
- Pie 初始化、資料刷新及選取變更時新增整體旋轉動畫，被選 slice 仍維持旋轉至上方並向外分離。
- 參考 Wijmo Sales Dashboard／palette 規格，將 Chart 預設配色改為柔和八色 RGBA palette，alpha 統一為 `.72`，並調整半透明資料點的選取對比。
- 依 Wijmo Selectable Slices 重做 Pie 選取：加入 `selectedItemPosition`、`selectedItemOffset`、`isAnimated`，使用目前角度到目標角度的最短路徑平滑旋轉，移除固定 -42 度重播效果。
- 移除 Pie slice 的 fade in／fade out 動畫；選取及刷新期間所有 slices 保持可見，只執行整體旋轉與選取 slice 位移。
- 將 Pie 選取旋轉時間由 `.48s` 調慢為 `.75s`，維持原 easing 與最短旋轉路徑。
- 修正點擊 Pie 後反向選取 Grid 造成相同 `selectPoint()` 二次重繪、提前中斷旋轉的問題；重複選取同一 point 現在不再刷新 SVG。
- Chart 對應 Grid selected row 的 Column、Bar、Line point 與 Pie slice 描邊統一改為 1px dotted SVG stroke。
- Chart selected row 對應資料的 dotted 描邊由 1px 調整為 2px，dash pattern 同步加寬為 `2 3`。
- Chart selected row 對應資料描邊由 2px dotted 改為 2px dashed，使用 `6 4` dash pattern 與平頭線段。
- Chart selected row 對應資料描邊調整為 1px dashed，使用 `4 3` dash pattern。
- Line Chart selected point 描邊改為 1px solid；Column、Bar 與 Pie selected 描邊繼續使用 1px dashed。
- Chart 新增可選 `selectionSource`（預設 `null`），傳入 FabGrid 後由元件管理雙向 selection 連動與 listener 清理；整合 Demo 移除手動事件橋接。
- 拉大 Grid + Chart 整合 Demo 各月營收、成本與訂單數差異，讓 Column、Line 與 Pie 的資料變化更明顯。
- Pie 新增可設定 `dataLabel`，支援 name/value/percent 模板與 function、Inside/Outside 位置；整合 Demo 先顯示 slice 百分比。
- 新增 `demo/grid-chart.es5.html`，只引用 build 後的 `dist/fabui.min.js`／`dist/fabui.css` browser global，範例腳本使用 ES5 語法並保留完整 Grid + Chart 功能。
- README Demo 清單分開列出 FabGrid + Chart 開發版與 ES5 Build 版，並註明各自引用 `src` ES module 或 `dist` browser global。
- 將本次 Chart 開發契約與踩坑整理進 `AGENTS.md`，涵蓋 Wijmo 式資料 API、selectionSource、資料監測、Pie 旋轉與 label、動畫禁用 fade、palette、selected 樣式、雙 Demo 與驗證規則。
- 依需求從 README 移除 FabGrid + Chart 開發版與 ES5 Build Demo 兩條連結，Demo 檔案仍保留。
- 將 `docs/api.md` 更名為 `docs/fabgrid-api.md`，並同步更新 README 內所有引用。
- 將 FabUI Chart API 的屬性／選項、方法與事件改為表格格式，補上型別、預設值、參數、回傳值與觸發時機。
- 修正 README 線上 Chart Demo 指向 `demo/grid-chart.es5.html`；Pages workflow 在 build 前加入 `npm ci`，解決 CI 找不到 `esbuild` 導致新 Demo 未發布的問題。

## 驗證

- `npm test`
- `node --check` 檢查 source、build、demo 與 test JavaScript。
- 在 `/tmp` 隔離副本執行 `npm run smoke`，完整通過且未改寫專案 `dist`。
- `npm run smoke` 重建正式 bundle，Chart 與既有 FabGrid 瀏覽器驗證全部通過。
