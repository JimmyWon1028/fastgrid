# 2026-07-11 工作紀錄

## 完成進度

- 專案品牌全面改名為 FabGrid，Fab 代表 fabulous。
- 對外公開 API 統一為 `fabui.FabGrid`：
  - browser global 為 `fabui.FabGrid`，不提供 `window.FabGrid`。
  - ES module 匯出 `fabui`，Grid 建構子為 `fabui.FabGrid`，語系 registry 為 `fabui.FabGridLocales`。
  - factory 維持內部實作，不作為公開 API。
- 對外引入檔案統一為 `dist/fabui.css` 與 `dist/fabui.js`；另提供對應的 minified 與 ESM bundle，不使用 `fabgrid.*` 作為發佈檔名。
- 更新 demo、smoke、build、AGENTS、README 與歷史工作紀錄中的品牌、路徑與範例。
- 重新產生 README 的 `fabgrid-logo.png`，移除 raster logo 中的舊名稱。
- GitHub repository 改名為 `JimmyWon1028/fabgrid`，本機 `origin` 與 GitHub Pages URL 同步為 FabGrid。
- 將 `.vscode/settings.json` 納入專案版本控制，統一 Live Server 使用 `5501` port，並修正為兩格空白縮排。
- 修正特殊群組鍵與危險 binding 路徑：群組狀態改用無 prototype dictionary，並阻擋可能污染物件 prototype 的 binding。
- 新增 `remote: false` 預設屬性；`remote: true` 僅保留為未來遠端資料模式契約，實作列入 TODO。
- 新增本地資料 Pagination，採 EasyUI DataGrid pager 的定義與樣式，支援 page size、首頁／上一頁／頁碼／下一頁／末頁、重新整理與筆數資訊。
- Pagination icon 改用 EasyUI default theme 的 `pagination_icons.png` sprite，並沿用原始 background-position 定義。
- 修正 Pagination sprite 顯示範圍：按鈕內使用固定 `16 × 16` icon span，避免露出相鄰圖示。
- Pagination 的 `showPageList` 預設為 `false`，並同步隱藏 pageList 右側分隔線。
- 中文 Pagination 頁碼文字調整為 `第 [頁碼] 共33頁` 格式，繁中與簡中同步更新。
- 中文 Pagination 頁碼前綴由「第」改為「頁」。
- 將 FabGrid 實際使用的 icon class 從 `my.icon.css` 移至 `fabgrid.icon.css`，並同步調整 Demo、smoke 與 build 產物名稱。
- Pagination DOM 改為放在 pager 容器內，`getPager()` 回傳外層 pager。
- Demo 的 `pageNumber`、`pageSize`、`pageList`、`showPageList` 改放在 `pager` 設定物件內，核心保留頂層寫法相容性。
- 新增 `pager.showPageInfo`，預設為 `true`；Demo 設為 `false` 隱藏右側分頁資訊。
- 新增 `pager.showRefresh`，預設為 `true`；設為 `false` 時同步隱藏重新整理按鈕與左側分隔線。
- Pager 基準字體調整為 `14px`。
- 中文 pageInfo 調整為 `顯示1到100,共447記錄` 的緊湊格式。
- 開始實作 `remote: true`：新增 Promise loader、EasyUI `{ page, rows }` 請求、`{ total, rows }` 回應、載入與錯誤事件；Demo 使用延遲切片模擬遠端資料。
- Remote 翻頁效果對齊 EasyUI 影片：頁碼先切換、保留舊資料、整個 grid 顯示半透明 loading mask，回應完成後再替換新頁。
- Remote processing overlay 改用 EasyUI default 的 `#ccc`、`0.3` 透明度，訊息框維持白底與 `#95B8E7` 邊框。
- Remote 新增內建 `url + method` 請求，支援 GET query string 與 POST form body；自訂 Promise loader 維持優先且向下相容。
- Remote 完成排序協定：傳送 EasyUI `sort`、`order`，排序時回到第 1 頁且不在前端重排單頁資料；Demo loader 同步模擬後端排序。
- Remote 完成篩選協定：全域搜尋傳送 `q`、欄位條件傳送 JSON `filterRules`，條件改變時回到第 1 頁，後端回傳篩選後 total；Demo loader 同步模擬後端篩選。
- Row number 背景調整為由 `#F9F9F9` 到 `#efefef` 的垂直漸層，並套用於一般、交錯、hover 與 selected 狀態。
- Header cell 與 footer cell 統一使用由 `#F9F9F9` 到 `#efefef` 的垂直漸層。
- Checkbox 欄的 header、body、footer 及各資料列互動狀態統一使用由 `#F9F9F9` 到 `#efefef` 的垂直漸層。
- Header cell 與 footer cell 改回白色高光加黑色陰影的半透明漸層，row number 與 checkbox 欄維持既有漸層。
- Header row 與 footer row 左側的 row-number、checkbox 兩格改用白色高光加黑色陰影的半透明漸層；body cell 維持垂直漸層。
- Search row 左側的 row-number、checkbox 兩格改用由 `#F9F9F9` 到 `#efefef` 的垂直漸層。
- Pager 背景改為 `#F4F4F4`，邊框色改為 `#dddddd`。
- 修正 theme 背景只覆寫 color、未清除共用 gradient 的問題；dark-hive 改用自身灰黑階層、亮色文字與深色 pager，避免淺色共用樣式造成不協調。
- Dark-hive row number 的一般、交錯、hover、selected 狀態統一為灰黑漸層與白字，row header pane 空白區同步改為深炭灰。
- Black theme row number 的一般、交錯、hover、selected 狀態統一為 `#666666` 到 `#444444` 漸層與白字，row header pane 空白區同步改為 black theme 灰色。
- Black theme 的 header、footer 文字由灰色提高為 `#e8e8e8`，改善深灰背景上的辨識度。
- Black theme search row 左側的 row-number、checkbox 兩格改為 `#666666` 到 `#444444` 漸層，避免沿用 default 淺色背景。
- Black theme pager 改用 `#555555` 背景、`#222222` 邊框與白字；頁碼輸入維持白底，分隔線及 hover 使用 black theme 灰階。
- 從 EasyUI `resource/themes/black` 複製白色 `pagination_icons.png` 為 `pagination_icons_dark.png`，black 與 dark-hive pager 改用暗色主題專用白色 icon sprite。
- 重整 source 目錄：`src/styles/themes` 移至 `src/theme`，FabGrid 使用的 `fabgrid.css`、`fabgrid.icon.css` 移至 `src/`，並同步修正圖片、Demo、build 與文件路徑。
- `my.icon.css` 最終移至 `demo/my.icon.css`，其使用的圖片移至 `demo/images`；FabGrid core、source、build、test 與 library 文件皆不包含或引用 `my.icon.css`。
- Demo 可額外載入 `demo/my.icon.css`；載入順序放在 `fabgrid.icon.css` 前面，library 與 build 不依賴這份 Demo CSS。
- 移除舊 build 遺留的 `dist/my.icon.css`、`dist/my.icon.min.css`；後續 build 會先清理 `dist/images`，確保 Demo 專用 icon 與圖片不會殘留在 FabGrid 產物。
- 清除 `src/theme/images` 與 `dist/images` 中未被 FabGrid CSS 引用的舊 EasyUI 圖片；FabGrid 圖片目錄只保留實際使用的 12 個檔案。
- 刪除已無實際檔案需求的 `src/styles`，build 主題流程改為只讀取 `src/theme`，README 移除舊 `src/styles/my.wijmo.*.css` 相容說明。
- 從 `resource/themes/metro-red/images/pagination_icons.png` 複製 metro-red pager sprite 到 `src/theme/images`，metro-red theme 的首頁、前頁、後頁、末頁與重新整理 icon 改用該主題圖檔。
- 依 `resource/themes/<theme>/pagination.css` 結構處理全部 16 個主題：各自建立 `src/theme/<theme>/images/pagination_icons.png`，Pager 五個 icon 改由對應 `fabgrid.<theme>.css` 定義，不再由 `fabgrid.icon.css` 集中覆寫 theme icon。
- Browser global namespace 改為 `fabui`，FabGrid 建構子使用 `fabui.FabGrid`；移除直接的 `window.FabGrid` 與 `window.FabGridLocales`，語系 registry 改放在 `fabui.FabGridLocales`。
- 開始拆分核心模組：新增 `src/fabui.js` 作為最上層 namespace 入口，原核心移至 `src/grid/fabgrid.js`；FabGrid 掛在 `fabui.FabGrid`，未加入未來的 textbox 控件。
- 參考 `resource/themes/*/images` 結構，將 `src/images` 全部移至 `src/theme/images`；同步修正 FabGrid、Demo icon CSS 與 build 圖片來源，原 `src/images` 已移除。
- 將 `src/resource` 移至專案根目錄 `resource/`，作為未來本機參考檔案區；加入 `.gitignore`，build、Git 與 GitHub 均不包含。
- Demo Grid 下方新增筆數與 OR 全域篩選列，和上方搜尋框雙向同步並提供清除按鈕；功能僅存在 `demo/`，未加入 FabGrid core。
- 修正 Demo 語系初始化早於 Grid 建立時，底部筆數讀取未初始化 `grid.options` 而拋錯；初始化階段先顯示 `-`。
- 新增獨立 `src/textbox` 子模組，TextBox 透過 `fabui.TextBox` 公開，不放入 FabGrid 核心。
- TextBox 參考 EasyUI default theme，使用 `#95B8E7` 邊框、5px 圓角、14px 字體、focus 淡藍光暈與半透明 icon 互動樣式。
- TextBox 支援 label、clear button、左右 icons、附加 button、自訂尺寸、multiline、fluid width、disabled、readonly、editable 與 value API。
- 新增 `demo/textbox.html` source-mode 展示頁，包含 Basic、Clear Icon、Custom、Icons、Button、Size、Multiline、Fluid 八種範例。
- Build 腳本已納入 TextBox JavaScript namespace 與 `textbox.css`／`textbox.min.css` 產物；本次依開發規則未執行 build，未更新 dist。
- Grid source-mode Demo 右下角篩選欄改用 `fabui.TextBox`，OR icon 與清除按鈕整合在同一個 TextBox 外框，並保留原有雙向搜尋、清除與語系切換行為。
- Demo Grid 下方 panel 的筆數、篩選與 OR 文字改為跟隨目前 Grid header 的字型、字級、字重與字色，切換主題時同步更新。
- 移除 Demo toolbar 上方重複的搜尋輸入框，搜尋統一由 Grid 右下方 `fabui.TextBox` 操作。
- 右下方篩選 TextBox 寬度改為 Grid／viewport 可用寬度的三分之一。
- 下方筆數改顯示分頁／篩選後的總筆數並加入千分位，例如 `筆數: 1,732`。
- FabGrid 新增 Excel 風格 `Page Up`／`Page Down` hotkey：active cell 保持同一欄並向上／向下移動一個可視頁面的列數，資料首尾會自動 clamp。
- FabGrid 新增跨平台同欄首尾 hotkey：Windows／Linux 為 `Ctrl + ↑/↓`；macOS 為 `Fn + Option + ↑/↓`（DOM 對應 `Option + PageUp/PageDown`）。
- FabGrid 新增跨平台同列左右邊界 hotkey：Windows／Linux 為 `Ctrl + ←/→`；macOS 為 `Fn + Option + ←/→`（DOM 對應 `Option + Home/End`）。
- macOS 同列左右邊界另支援 `Fn + ←/→`（DOM 對應無 modifier 的 `Home/End`），輸入框及 cell editor 不攔截。
- 新增獨立 `src/numberbox` 子模組，透過 `fabui.NumberBox` 公開並組合既有 TextBox，不加入 FabGrid 核心。
- NumberBox 依 EasyUI 1.11.5 NumberBox 契約支援 `min`、`max`、`precision`、`decimalSeparator`、`groupSeparator`、`prefix`、`suffix`、`parser`、`formatter`、`filter` 與 `onChange`。
- NumberBox 支援數字鍵過濾、paste／自動填入清理、Enter／blur fix、range clamp、precision 固定小數、原始值與格式文字分離。
- 新增 `demo/numberbox.html`，展示 Basic、Number Range、Format NumberBox、Fluid NumberBox；build 腳本已納入 NumberBox JS 與 CSS，但本次未執行 build。
- 新增獨立 `src/datebox` 子模組，透過 `fabui.DateBox` 公開並組合既有 TextBox，不加入 FabGrid 核心。
- DateBox 依 EasyUI DateBox 契約支援下拉 Calendar、Today／Close 與自訂按鈕、日期 formatter／parser、日期 validator、onSelect／onChange、共享 Calendar、cloneFrom 與 fluid width。
- DateBox 內建 `en`、`zh-TW`、`zh-CN` 的月份、星期與按鈕文字；新增 `demo/datebox.html` 展示主要應用。Build 腳本已納入 DateBox JS 與 CSS，本次未執行 build。
- 新增獨立 `src/combobox` 子模組，透過 `fabui.ComboBox` 公開並組合既有 TextBox，不加入 FabGrid 核心。
- ComboBox 依 EasyUI ComboBox 契約支援 input／select、單選／多選、輸入篩選、群組、formatter、item icon、limitToList、鍵盤導覽、本地資料與 GET／POST／Promise loader 遠端資料。
- 新增 `demo/combobox.html` 展示 Basic、Multiple、Group、Custom Format、Item Icons、Remote Data、Actions、Multiline 與 Fluid；build 腳本已納入 ComboBox JS 與 CSS，本次未執行 build。
- 抽出 `src/editor/editor-definitions.js`，FabGrid、TextBox、NumberBox 改為共用同一份 editor 定義，並由 `fabui.editorDefinitions` 與 `FabGrid.editorDefinitions` 公開。
- NumberBox 改用 FabGrid numberbox editor 的輸入清理、即時千分位、precision、解析、複製去千分位與 editor class；保留 min／max、decimalSeparator、groupSeparator、prefix／suffix 等 standalone 延伸選項。
- DateBox 改用 FabGrid datebox editor 的 `textbox-f / datebox-f / fg-editor-datebox` class、numeric inputMode、預設 `9999/99/99` mask、日期輸入清理、`yyyy/MM/dd` 顯示與 ISO data value；Calendar、formatter／parser、validator、sharedCalendar 功能維持獨立。
- 新增 `fabui.YymmBox` 與 `src/yymmbox` 子模組，使用共用 `editorDefinitions.yymmbox`；預設顯示 `YYYY/MM`、資料值為 `YYYYMM`，popup 固定為年份／月份選擇，FabGrid `yymmbox` editor 同步改用相同的清理、格式化、解析、class 與刪除規則。
- 新增 `demo/yymmbox.html` source-mode 範例與 smoke 檢查；build 腳本已納入 YymmBox JavaScript 與 CSS，本次依開發規則未執行 build。
- 調整 source 公開入口為 `src/fabui.js` 與 `src/fabui.css`；Grid、editor、各控件、locale 與 theme 仍保留清楚的內部分層。
- Build 每次先清空 `dist`，輸出 `fabui.js`、`fabui.min.js`、`fabui.esm.js`、`fabui.esm.min.js`、`fabui.css` 與 `fabui.min.css`。
- `fabui.js` 僅公開 `fabui.FabGrid`、`fabui.FabGridLocales` 與必要的 `fabui.editorDefinitions`；`fabui.css` 僅整合 FabGrid 所需的 core、icon 與 theme 樣式。
- Demo 與 smoke 改用單一 FabUI JS／CSS bundle，不再動態載入 `dist/themes`、`dist/locales` 或個別控件 CSS。
- Build 驗證時同步修正共用日期 mask 刪除到 literal 邊界時未保留尾端分隔符的 regression，DateBox 與明確日期 mask 的 Backspace 行為恢復一致。
- Build 提供自包含的 `fabui.esm.js` 與 `fabui.esm.min.js`；ESM 與 browser global 版本皆以 `fabui.FabGrid` 作為唯一 Grid 建構子，`dist` 輸出契約共六個主要檔案。
- Build 恢復輸出 `dist/theme` 依賴，包含各主題可讀／壓縮 CSS、共用圖片與各主題 Pager sprite；`fabui.css` 改用可部署的相對資源路徑，不再把圖片內嵌為 data URI。
- 移除 FabGrid search row 左側 row header／selection header 的篩選筆數顯示；左側 search row 空白區的雙擊清除行為保留，Pager page info、row number 與 Demo 下方筆數維持不變。
- Demo 右下角篩選改為逗號分隔的 OR 條件，例如 `aaa,bbb,ccc`；任一關鍵字符合任一資料欄位即保留該列，邏輯只存在 `demo/demo.js`，未加入 FabGrid 核心。
- Demo 右下角篩選的 `OR` icon 可點擊切換為 `&`；OR 模式任一條件符合即可，AND 模式要求每個逗號條件都能在該列的任一欄位找到，再次點擊可切回 OR。
- Demo 篩選的 `&` icon 字級調整為 18px，使其視覺大小與 `OR` 接近。
- `fabui.TextBox` icon callback 改用 `icons[].onClick(e)`，事件提供 `e.data.target` 原始 input、`e.data.textbox` instance、icon 與 index；舊 `handler` 保留相容，DateBox、ComboBox 與 Demo icon 定義同步改用 `onClick`。
- 新增獨立 `fabui.Tabs` 與 `src/tabs` 子模組，參考 EasyUI default Tabs theme，支援 markup/programmatic tabs、add/select/close/update、enable/disable、tools、overflow、鍵盤導覽、四向 tabPosition、plain/narrow/pill/justified、fixed/fluid/auto-height、nested 與 href 載入。
- 新增 `demo/tabs.html` source-mode Demo，涵蓋 Basic、Auto Height、Nested、Tabs Tools、Fixed Width、Justified、Tabs Style、Tab Position、Fluid Tabs；Tabs 內建 `en`、`zh-TW`、`zh-CN` UI 文字。
- `demo/tabs.html` 新增與 FabGrid 相同的 16 組主題切換；每個主題依 `resource/themes/<theme>/tabs.css` 建立獨立 Tabs 色票，並複製對應 `tabs_icons.png` 到 `src/theme/<theme>/images`，切換只套用到各 Tabs instance。
- TextBox、NumberBox、DateBox、YymmBox、ComboBox demo 新增共用的 16 組主題切換；依 `resource/themes/<theme>` 的 `textbox.css`、`combo.css`、`combobox.css`、`calendar.css`、`datebox.css` 建立各主題控件色票，輸入框、按鈕、下拉清單與日曆 popup 會同步切換。
- 修正 Material 系列主題：Tabs 依 material 4px、material-blue 2px、material-teal 4px 套用正確圓角；Material Blue TextBox button 使用 `#2196f3`，Calendar 分離 selected 背景與藍色邊框，三套主題改用各自的 ComboBox、DateBox、Calendar 圖片。
- `fabui.TextBox` 新增 `autoUnmask` option，預設值為 `true`；DateBox 既有的明確 mask copy 契約維持不變。
- Build 新增 CSS 圖片依賴完整性檢查與 standalone component 洩漏檢查；filter popup 的清除圖示定義移至 `fabgrid.icon.css`，並驗證輸出路徑固定為 `dist/theme/images/clear.png`。
- 發佈範圍收斂為 FabGrid：`src/fabui.js` 與 `dist/fabui.*` 僅公開／包含 `fabui.FabGrid`、`fabui.FabGridLocales` 與必要的 `fabui.editorDefinitions`。
- TextBox、NumberBox、DateBox、YymmBox、ComboBox、Tabs 保留原始碼但改列為 TODO，不再編譯進 FabUI JavaScript／CSS bundle；README 與 TODO 已同步更新。
- Grid Demo 的底部篩選列改回原生 input／button，不再依賴 TextBox；工具列新增「分頁」與「遠端」checkbox，可切換本機分頁與既有 Promise loader 遠端模式。
- Demo 預設狀態調整為繁體中文、Default theme、左凍結 2、右凍結 1、顯示列號、無群組，並關閉搜尋列、分頁、遠端、多選與編輯；資料列數預設為 1732。

## 驗證

- 全專案文字與檔案路徑掃描確認不再含舊品牌拼寫。
- `node --input-type=module -e "import('./src/fabui.js')"` 通過。
- `node --check build/build.cjs`、`node --check build/smoke.cjs`、`node --check demo/demo.js` 通過。
- `node --check src/textbox/textbox.js`、`node --check demo/textbox-demo.js` 通過；source-mode 瀏覽器驗證 clear、button、multiline 與 fluid width 通過。
- `npm run smoke` 通過，並重建 `dist`。
- GitHub repository 與 Pages 已確認：
  - https://github.com/JimmyWon1028/fabgrid
  - https://jimmywon1028.github.io/fabgrid/

## 備註

- `fg-*` CSS class 保留，作為 FabGrid 的簡寫，且不包含舊品牌名稱。
