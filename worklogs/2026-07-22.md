# 2026-07-22

## 完成進度

- FabGrid `remote: true` 的相容 `op` 補齊比較符號映射：`>=`、`>`、`<=`、`<`、`<>`、`=` 分別對應 Search Row 的 `gte`、`gt`、`lte`、`lt`、`ne`、`eq`；既有萬用字元符號維持不變，遠端 request 仍保留原始 `op`。`in` 的陣列序列化明確支援不分大小寫並保留輸入大小寫，Search Row 顯示 `IN`；同步更新 README、AGENTS 契約與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：以目前 source 重建 FabUI Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認版本為 `2026.7.22`、包含最新 remote filter 相容符號，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 修正 `remote: true` 的空值初始化規則：對應 column 的相容 `op` 即使 `value: ""`，仍會初始化 Search Row operator 與 filter icon；空白 Search input 不會產生或送出該欄位的篩選規則，輸入內容後才沿用原始 `op`，再次清空即停止篩選。本批同步更新 Grid source-mode cache version、README、AGENTS 契約與回歸測試，未重建 `dist`。
- 再次完成 `build lite min`，將 `remote: true` 空白值不篩選、輸入後沿用預設相容 `op` 的最新規則編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認版本 `2026.7.22`、語法有效，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 調整 FabGrid Header sort 視覺：排序箭頭改為在既有 `22px` 容器內靠底排列並保留 `4px` 底距，使符號下緣與欄位文字行框下緣對齊；多欄排序序號仍位於箭頭上方。同步更新 source-mode JS／CSS cache version、README／AGENTS 契約與 browser smoke 幾何檢查，本批未重建 `dist`。
- 再次完成 `build lite min`，將 Header sort 符號下緣與文字下緣 `0px` 對齊的最新樣式編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認壓縮 CSS 含 `padding-bottom: 4px`／`justify-content: flex-end`，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 新增獨立 Theme CLI build：`npm run build:theme` 只重建 `dist/theme/` 的 19 組一般／壓縮 CSS 與必要圖片，`npm run build:theme -- min` 只保留壓縮 CSS 與圖片；core 與獨立 Theme build 共用 `build/theme-builder.cjs`，並新增暫存輸出回歸測試，確認 Theme build 不影響其他 bundle。本批未重建工作區 `dist/theme/`。
- 完成 `build theme min`：以獨立 `npm run build:theme -- min` 重建工作區 `dist/theme/`，最終保留 19 組 `fabui.<theme>.min.css`、72 個 Mono SVG 與各 theme 必要圖片，確認沒有非壓縮 theme CSS；build 前後所有 `dist/` 頂層 bundle 雜湊一致，未改動其他發佈範圍。
- 重整 Theme 發佈架構：`fabui.css`／`fabui.min.css` 與 Lite CSS 只包含共用樣式及 Default 配色，其他 18 組 Theme 改為固定 component selector 的獨立 CSS，不再依賴 `fg-theme-*` class；Demo 選擇 theme 時改由 query string 載入外部 Theme CSS 並重新載入頁面。同步調整 Theme builder、Mono SVG 繼承、README、AGENTS 與回歸測試。
- 完成最終 `build all`：重建 FabUI core、Lite、Diagram、Gantt、Scheduler 與 Theme，輸出 18 組一般版及 18 組壓縮版外部 Theme CSS，不另產生 Default Theme 檔或任何 ESM 檔；488 項單元測試、core／Gantt／Scheduler browser smoke、CSS 資產與 `git diff --check` 全部通過。
- 修正窄欄位 Header 的 filter icon 清晰度：文字內容固定在 icon 左側裁切，不再延伸到 icon 下方；一般 funnel 與 active operator icon 均向右微調 `2px`，並保留原有 hit area。同步更新 source cache version、README、AGENTS 與 browser smoke 幾何契約，本批未重建 `dist`。
- 修正 filter icon 間距調整方向：還原窄欄位 Header 的 `24px` 文字裁切保留區，改將 funnel 圖形在 hit area 內的左側內距由 `4px` 縮小為 `2px`，stem 同步左移 `2px`；保留 icon 的右側 `-2px` 定位、hit area 與 Header resize handle 行為，本批未重建 `dist`。
- 依實際 icon 幾何修正窄欄位 sort／filter 間距：排序啟用時為 Header 增加 sorted 狀態，將 sort icon 固定到 filter funnel 左側並維持 `2px` 可視間距，文字則裁切於 sort icon 左側；一般寬度或未排序 Header 維持原排列，本批未重建 `dist`。
- 加深 Default 主題切換運算符後的 filter icon 顏色：active operator 由較淺的 `#3b82f6` 改為標準 `blue`（`#0000ff`），一般漏斗與 Excel-like active 狀態維持原樣；同步更新 source cache version、README、AGENTS 與樣式回歸測試，本批未重建 `dist`。
- 修正 `remote: true` 的 Excel-like 值篩選候選值：首次開啟時快取欄位完整候選值，套用後即使遠端資料只回傳已勾選值，重新開啟同一欄位仍顯示原完整集合，並以目前 filter values 還原勾選狀態；同步更新 source cache version、README、AGENTS 與回歸測試，本批未重建 `dist`。
- 調整 Default 主題的運算符 filter icon：維持標準 `blue`（`#0000ff`），字重由 `bold` 改為 `normal`，不再加粗；同步更新 source CSS cache version、README、AGENTS 與樣式回歸測試，本批未重建 `dist`。
- 修正 `build:lite` 對尾端 `min` 參數的處理與 browser-global 壓縮方式：`npm run build:lite -- min` 現在只產生並保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，不再留下 Lite 非壓縮檔，且壓縮版可直接公開 `fabui` API；新增隔離輸出／執行回歸測試並重新完成 `build lite min`。
- 修正 Diagram 獨立發佈檔名契約：公開物件為 `fabui.Diagram`，因此 bundle 統一改為 `dist/fabui.diagram.js`、`dist/fabui.diagram.min.js`、`dist/fabui.diagram.css`、`dist/fabui.diagram.min.css`；同步更新 build script、正式 Demo、smoke、README、AGENTS 與回歸測試，本批未重新編譯 `dist`。
- 統一運算符 filter icon 的主題色：Default 與其餘淺色主題使用一般字重 `blue`，`black`／`dark-hive` 深色主題使用白色；改由 `--fg-filter-operator-color` 共用變數控制，補齊主題回歸測試與 source cache version，本批未重建 `dist`。
- 統一 FabGrid 欄位篩選設定為 `filterMode: false | Array<'excel' | 'searchRow'>`：陣列第一項為目前模式，單一項目只開放該模式，空陣列或無效值關閉兩者；移除 `showSearchRow` option、method 與 event，更新 core、TreeGrid、Pivot、jQuery／Vue wrapper、Demo、locale、README／API 文件與回歸測試。`filterRules` 只在 Search Row 為可用模式時處理；本批未重建 `dist`。
- 補強 `filterMode` 順序契約：正規化後陣列第一項為預設及目前篩選模式，只有 `array.length > 1` 時 Header 右鍵功能表才顯示篩選方式切換；單一模式、`false` 與空陣列都不顯示切換項目。同步補齊純函式回歸測試與文件，本批未重建 `dist`。
- 完成 `build lite min`：將最新 `filterMode` 單一設定、第一項預設模式及 `array.length > 1` 才提供右鍵切換的規則編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認無 Lite 非壓縮檔及 ESM 發佈檔，Lite bundle 測試 2/2 通過。
- 完成 `build clear`：清除 `dist/` 下所有 bundle、theme 與圖片內容，保留空的 `dist/` 目錄，未執行任何編譯。
- `build clear` 後再次完成 `build lite min`：從空的 `dist/` 只重建 `fabui.lite.min.js`、`fabui.lite.min.css` 與必要圖片，確認無非壓縮 Lite／ESM 檔，語法有效且 Lite bundle 測試 2/2 通過。
- 完成 `build theme min`：只重建 `dist/theme/`，輸出 18 組非 Default 外部 Theme 壓縮 CSS、72 個 Mono SVG 與各主題必要圖片；確認無非壓縮 Theme CSS／ESM 檔，Theme build 回歸測試通過，既有 Lite JS／CSS 雜湊保持不變。
- 修正 FabGrid runtime layout 正規化會把 `filterMode` 重設為預設值的問題：Header 右鍵現在可在 Excel-like／Search Row 間切換，上方「篩選」開關也能正確停用與重新啟用篩選；`dev-grid.html` 改為保存新的 `filterMode` 狀態並相容舊 `allowFiltering`／`showSearchRow` localStorage 設定。本批未重建 `dist`。
- 完成 `build lite min`：將最新 `filterMode` runtime 切換與上方篩選開關修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認版本 `2026.7.22`、無 Lite 非壓縮檔及 ESM 發佈檔，Lite／build scope 回歸測試 9/9 通過。
- 調整窄欄位 Header 文字顯示：判定欄寬不足時不再為 filter／sort icon 預留右側裁切空間，文字可延伸並重疊顯示於 icon 下方；一般寬度、icon 固定位置、排序間距、點擊區與 resize handle 層級維持不變。同步更新 README、AGENTS、source cache version 與 CSS／browser smoke 回歸契約，本批未重建 `dist`。
- 完成 `build lite min` 與 `build theme min`：Lite 已編入窄 Header 文字可重疊 filter／sort icon 的最新樣式，只保留 `fabui.lite.min.js`／`.min.css`；Theme 輸出 18 組非 Default 壓縮 CSS 與必要圖片。確認無 Lite／Theme 非壓縮檔、無 ESM 檔，版本 `2026.7.22`，Lite／build scope 回歸測試 9/9 通過。
- 修正右對齊窄 Header 在切換排序時的文字位移：右對齊 Header 固定保留 `24px` 右側空間，不再因 sort icon 讓窄欄位判定改變而向右跳動；一般左對齊窄欄位仍可讓文字重疊 filter／sort icon。以 60px「米數」Header 實測排序前後文字 `left` 相同、位移 `0px`，同步更新 README、AGENTS、source cache version 與 browser smoke 契約，本批未重建 `dist`。
- 完成 `build lite min`：將右對齊窄 Header 排序前後文字位置固定的最新樣式編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認版本 `2026.7.22`、無 Lite 非壓縮檔及 ESM 發佈檔，Lite／build scope 回歸測試 9/9 通過。
- 修正數字／右對齊窄 Header 的 filter icon 重疊規則：移除右對齊欄位的保留空間，使文字與一般文字欄位相同，可延伸並重疊顯示於 filter icon 下方；窄欄位判定固定預先計入 sort icon 寬度，確保排序前後文字位置不跳動。以 60px「米數」Header 實測排序前後皆為 `padding-right: 0`、重疊成立且文字位移 `0px`；同步更新 README、AGENTS、source cache version 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將數字／右對齊窄 Header 文字可重疊 filter icon、排序前後位置固定的最新修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認壓縮 CSS 包含窄 Header `padding-right: 0`、JavaScript 語法有效，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 調整右對齊數字 Header 的自適應外側間距：完整標題仍可顯示時，右側保留與左對齊文字 Header 左側相同的 `6px`；空間不足時自動降為 `0px`，讓文字繼續延伸至 filter icon 下方。瀏覽器實測 60px「米數」右側為 `6px`、35px「顆數」為 `0px`，前者排序前後間距與位置位移皆為 `0px`；同步更新 README、AGENTS、source cache version 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將右對齊數字 Header 自適應 `6px／0px` 外側間距修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認壓縮 JS／CSS 包含最新判定與樣式、JavaScript 語法有效，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 修正足夠寬的右對齊數字 Header 仍與 filter icon 重疊：改為三段式判定，欄寬足以容納完整標題與 icon 時使用 `24px` icon 空間且不重疊，稍窄時保留 `6px` 外側間距並允許重疊，再窄時才降為 `0px`。右對齊 sort icon 改為絕對定位，維持排序前後文字位置不變；瀏覽器實測 60px「稅額」文字與 filter／sort icon 均無重疊且排序位移 `0px`，50px／35px 欄位分別使用 `6px`／`0px`。同步更新 README、AGENTS、source cache version 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將右對齊數字 Header 的 `24px／6px／0px` 三段式間距、足夠寬時避開 filter icon，以及排序位置固定修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認壓縮 JS／CSS 包含最新 class 與絕對定位規則、JavaScript 語法有效，且沒有 Lite 非壓縮檔或 ESM 發佈檔。
- 調整 `remote: true` 的 `filterRules.op` 送出契約：GET／POST request 與 `getFilterRules()` 不再送出已知標準名稱，文字 operator 統一轉為 `..%`、`%..%`、`%..`、`!..%`、`!%..%`、`!%..`，比較 operator 統一轉為 `>=`、`>`、`<=`、`<`、`<>`、`=`；既有相容符號保持不變，沒有對應符號的自訂 `op` 與不分大小寫的 `in` 仍原樣送出。同步更新 Demo 模擬後端、source cache version、README、AGENTS 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將 `remote: true` 的文字與比較 `filterRules.op` 符號化送出規則編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認 JavaScript 語法有效、無 Lite 非壓縮檔或 ESM 發佈檔，remote filter 回歸測試 74/74、build scope 測試 7/7 通過。
- 將 FabGrid Search Row 的 `searchDelay` 預設值由 `200ms` 調整為 `500ms`，本機與 `remote: true` 共用相同 debounce；同步更新 Grid／Vue Demo、README、AGENTS 契約與預設值回歸測試。完整測試 506/506 通過，本批未重建 `dist`。
- 修正遠端大量資料載入後，垂直捲動軸使 client width 變窄時誤出現橫向捲動軸：Size layer 的 inline width 改為只使用實際欄位總寬，空間有餘時交由 CSS `min-width: 100%` 跟隨即時可用寬度；瀏覽器實測 `clientWidth`／`scrollWidth` 同為 `433px`、橫向捲動軸隱藏，完整測試 507/507 通過。同步更新 source cache version、README、AGENTS 與回歸測試，本批未重建 `dist`。
- 修正 Search Row input 的 `Enter`／`Shift+Enter` 行為：兩者不再取消 debounce timer 或立即套用／送出遠端查詢，僅攔截按鍵與外層表單預設提交；輸入內容仍依 `searchDelay` 套用，`Tab` 導覽維持不變。瀏覽器模擬確認兩種按鍵後 100ms 的 request count 均不變，500ms 到期後才各增加一次，完整測試 508/508 通過；同步更新 source cache version、README、AGENTS 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將 Size layer 多餘橫向捲動軸修正及 Search Row `Enter`／`Shift+Enter` 不立即送出查詢的最新行為編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認 JavaScript 語法有效、無 Lite 非壓縮檔或 ESM 發佈檔，Lite／Grid 相關測試 84/84 通過。
- 修正上一版 Search Row 按鍵處理遺漏導覽的問題：`Enter`／`Tab` 恢復移到下一個可見欄位 input，`Shift+Enter`／`Shift+Tab` 移到上一個；四者都保留既有 debounce timer，不立即套用或送出遠端查詢。同步更新 source cache version、README、AGENTS 與四種按鍵回歸測試；本批未重建 `dist`，前一版 Lite min 尚未包含本次修正。
- 將 Search Row `searchDelay` 預設值由 `500ms` 調整為 `400ms`，同步更新 Grid／Vue Demo、source cache version、README、AGENTS 與預設值測試。瀏覽器實測 `Enter`／`Tab` 分別移至下一個可見欄位、`Shift+Enter`／`Shift+Tab` 分別移至上一個，四次按鍵後 request count 均維持不變，400ms 到期後才增加一次，完整測試 508/508 通過；本批未重建 `dist`。
- 再次完成 `build lite min`：將 Search Row 四種按鍵導覽與 `400ms` 預設 debounce 編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認無 Lite 非壓縮檔或 ESM 輸出、語法有效，Lite／Grid 測試 84/84 通過。壓縮版瀏覽器實測四種焦點方向正確、按鍵當下不增加 request，400ms 後才送出一次查詢。
- 調整 Search Row 欄位導覽焦點：透過 `Enter`／`Tab`／`Shift+Enter`／`Shift+Tab` 移到下一個或上一個可見欄位時，若目標 input 已有內容就全選，空白 input 維持一般游標。瀏覽器實測「使用」選取 `0–2`、「A001」選取 `0–4`，四種按鍵方向皆正確，完整測試 509/509 通過；同步更新 source cache version、README、AGENTS 與回歸測試，本批未重建 `dist`。
- 再次完成 `build lite min`：將 Search Row 導覽到有內容的目標 input 時自動全選的修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認無 Lite 非壓縮檔或 ESM 輸出、JavaScript 語法有效，Lite／Grid 相關測試 85/85 通過。
- 修正 FabGrid constructor options 只有 `updatedView` 被綁定的問題：建立 Wijmo-compatible events 後改為逐一綁定所有同名 option callback，使 `selectionChanged: (g, e) => {}` 與其他公開事件可正常觸發。瀏覽器實測 callback 觸發 1 次、`g` 為原 Grid instance、`e.row/e.col` 為 `1/1`，完整測試 510/510 通過；同步更新 source cache version、README、API 文件、AGENTS 與回歸測試，本批未重建 `dist`。
- 再次完成 `build lite min`：將所有公開事件的 constructor option callback 綁定修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認無 Lite 非壓縮檔或 ESM 輸出、語法有效，Lite／Grid 相關測試 86/86 通過。壓縮版瀏覽器實測 `itemsSourceChanged` 初始不觸發，呼叫 `setItemsSource([{id:2},{id:3}])` 後觸發 1 次、sender 正確且 `e.rows` 為新資料。
- 修正凍結欄後方的非凍結欄寬於可用視窗時，點選該欄會錯誤向右捲動並讓欄位內容被凍結區遮住的問題；`scrollIntoView()` 現在優先把過寬欄位的左緣對齊凍結欄右側。窄版 source-mode 瀏覽器實測點選第二欄後 `scrollLeft` 維持 `0`、文字左側可見，完整測試 511/511 通過；本批未重建 `dist`。
- 完成 `build lite min`：將凍結欄後方過寬欄位優先左對齊的最新修正編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認 JavaScript 語法有效、無 Lite 非壓縮檔或 ESM 輸出，Lite／build scope 回歸測試 9/9 通過。
- 補充 FabGrid 欄位顯示切換契約：`setColumnVisible()` 的第一個參數接受 `grid.columns` index 或 column object；依 `binding` 操作時先用 `getColumn(binding)` 取得 object，binding 字串不可直接傳入。README 與 AGENTS 同步記錄不得只修改 `column.visible` 而略過 layout／選取／畫面更新；本批未修改 source、未重建 `dist`。
- 完成 `build all`：依序重建 FabUI core、Lite、獨立 Diagram、Gantt 與 Scheduler，輸出 20 個頂層 JS／CSS bundle、36 個外部 Theme CSS 與 72 個 Mono SVG；確認版本 `2026.7.22`、Diagram 使用 `fabui.diagram.*`、所有 JavaScript 語法有效，且沒有舊 `diagram.*` 或 ESM 發佈檔。完整測試 511/511、core／Gantt／Scheduler browser smoke 與 `git diff --check` 全部通過。
