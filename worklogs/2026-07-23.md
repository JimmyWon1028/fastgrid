# 2026-07-23

## 完成進度

- 同步更新 README、AGENTS 與工作日誌：README 補上 Grid 捲動／排序／遠端取消的現行效能行為及 `npm run benchmark:grid` 使用方式；AGENTS 固定 binding cache、排序值預先準備、垂直捲動靜態區重用、遠端 Fetch 取消、TreeGrid `WeakSet` 同步與 20,000×50 benchmark 驗證契約。
- 完成 `build all`：依序重建 FabUI core、Lite、Diagram、Gantt、Scheduler 與 HtmlEditor 的一般版及壓縮版 JS／CSS，版本為 `2026.7.23`；外部主題輸出 18 組一般版與 18 組壓縮版 CSS，Mono 保留 72 個 SVG，確認沒有任何 ESM 發佈檔。完整測試 551/551、core／Gantt／Scheduler browser smoke、全部發佈 JavaScript 語法及 `git diff --check` 均通過。
- 完成全庫第二輪錯誤、重構與效能審查：FabGrid nested binding path 改為有上限的快取，local／TreeGrid 多欄排序先為每列準備排序值，避免 comparator 重複拆 path、轉 Date 與 lowercase；TreeGrid 收合查詢改用同步 `WeakSet`。新增 `npm run benchmark:grid`，固定以 20,000×50、共 100 萬資料格量測 binding、全域搜尋、雙欄排序與虛擬化上限。
- 精簡 Grid 捲動熱路徑：scroll render 重用現有 layout，垂直捲動且水平欄位範圍未變時不重建 Header／Footer，也不重畫 Pager；內建遠端 Fetch 在較新 load 開始或 Grid dispose 時會中止舊 request。Chart nested binding 同樣改用有上限的 path cache，PivotWorkspace `pointercancel` 會恢復起始 pane／chart 尺寸且不提交變更。
- 完成第二輪效能與回歸驗證：可重複 benchmark 的中位數為 binding scan 2.72ms、全域搜尋 65.42ms、雙欄排序 18.99ms；source-mode 真實瀏覽器載入 20,000×50 Grid 時只建立 1,054 個資料 cell，垂直捲動 Header 重繪 0 次、水平欄位範圍變更時重繪 1 次，console 無 error。完整測試 551/551 與 `git diff --check` 通過，本批未重建 `dist`。
- 完成全庫第一輪錯誤、重構與效能審查：Chart 在 `observeData: false` 時不再建立定時輪詢，runtime 切換 observation／interval 會正確重啟或停止；Grid popup 的 document pointer listener 改為只在右鍵選單、Filter 或欄位選擇器開啟期間綁定，支援 `ResizeObserver` 時也不再重複綁定 `window.resize`。
- 修正跨元件非同步與互動生命週期：Gantt 的 Splitter／Task pointer listener 由共用 interaction lifecycle 管理，`pointercancel`／`dispose()` 會立即解除並放棄預覽；Tabs 與 Panel 會忽略已關閉、已銷毀或被新 request 取代的遠端回應，並在可用時取消舊 Fetch。
- 修正 HtmlEditor 對話框與圖片讀取競態：舊對話框的延遲 close 不會誤關新對話框，FileReader 在對話框關閉或 editor dispose 時中止且不再回寫已銷毀 DOM；`selectionchange` 只在可視化編輯區聚焦期間監聽。同步補齊 cache query version 與回歸測試，完整測試 545/545 通過，本批未重建 `dist`。
- 完成 source-mode 實際瀏覽器驗證：Grid 欄位選擇器可由外部點擊關閉、Filter 可由 `Escape` 關閉，Gantt 階層互動、HtmlEditor 圖片對話框與 PivotChart 均正常，四頁皆無 console error。
- 完成 `build lite min`：將右鍵選單 `filterMode`、`showRowHeaderMenu`、`showFullscreenMenu` 的最新顯示規則編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認兩個新 option 預設皆為 `false`、無 Lite 非壓縮檔或 ESM 發佈檔、JavaScript 語法有效，Grid／Lite／build scope／config／remote 測試 110/110 通過。
- 新增 FabGrid `showFullscreenMenu` option，預設為 `false`；只有設為 `true` 時，Header cell 右鍵選單才顯示 Grid 全螢幕／離開全螢幕項目，不影響 `toggleFullscreen()` API 或其他選單功能。同步更新 source cache version、README、API 文件、AGENTS 契約與實際選單 DOM 回歸測試，Grid 測試 94/94、完整測試 540/540 通過，本批未重建 `dist`。
- 新增 FabGrid `showRowHeaderMenu` option，預設為 `false`；只有設為 `true` 時，Header cell 右鍵選單才顯示「列號」子選單。隱藏此項目不改變目前列號欄狀態或 `setShowRowHeaders()` API，其他選單項目維持不變。同步更新 source cache version、README、API 文件、AGENTS 契約與實際選單 DOM 回歸測試，Grid 測試 94/94、完整測試 540/540 通過，本批未重建 `dist`。
- 修正 Grid 右鍵選單：`filterMode: false` 時不再顯示「清除篩選」，啟用任一 filter mode 時維持顯示；列號、Excel／CSV 匯出與全螢幕項目不變。同步更新 source cache version、README、API 文件、AGENTS 契約與實際選單 DOM 回歸測試，Grid 測試 94/94、完整測試 540/540 通過，本批未重建 `dist`。
- 完成 `build lite min`：將 `allowMultiSorting` 與遠端成功回應觸發 `itemsSourceChanged` 的最新行為編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認無 Lite 非壓縮檔或 ESM 發佈檔，JavaScript 語法有效，Grid／Lite／build scope／config／remote 測試 109/109 通過。
- 補齊遠端資料更新事件：初次載入、換頁、排序、篩選或 `reload()` 的有效成功回應套用新 rows 後，會以 `{ rows, remote: true }` 觸發 `itemsSourceChanged`，順序位於 `selectedRowChanged` 與 `loadSuccess` 之前；失敗、取消或過期 response 不觸發。同步更新 source cache version、README、API 文件、AGENTS 契約與回歸測試，Grid 測試 93/93、完整測試 539/539 通過，本批未重建 `dist`。
- 新增 FabGrid `allowMultiSorting` option，預設為 `true`；設為 `false` 時，Header 的 `Shift + 點擊` 與 `toggleSort(colIndex, true)` 都退回單欄排序，只保留目前操作欄位，排序事件的 `multiSort` 同步為 `false`。同步更新 source-mode cache version、README、API 文件、AGENTS 契約與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將 `fabui.setConfig()`／`fabui.getConfig()` 與全域 `request.credentials` 優先規則編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認無 Lite 非壓縮檔或 ESM 發佈檔，JavaScript 語法有效，針對性測試 16/16、完整測試 537/537 通過。
- 新增 `fabui.setConfig(options)`／`fabui.getConfig()` 全域設定 API，目前支援 `request.credentials`；全域值作為之後建立之 FabGrid 內建 `url` 請求預設，單一 Grid 的 `credentials` 優先，自訂 loader 不套用。`getConfig()` 回傳獨立副本，並同步 core／Lite build 入口、source cache version、README、API 文件、AGENTS 契約與回歸測試。
- 完成 `build lite min`：將內建遠端請求的 `credentials` option 編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認無 Lite 非壓縮檔或 ESM 發佈檔，JavaScript 語法有效，Lite／build scope／remote 測試 14/14 通過。
- 新增 FabGrid 公開 `credentials` option，預設為 `'same-origin'`，另支援 `'include'` 與 `'omit'`；內建 `url` 模式會將此設定傳給 Fetch，使用 `'include'` 時等同 jQuery `xhrFields.withCredentials: true`，可帶送跨來源 PHP Session Cookie。同步更新 source-mode cache version、README、API 文件、AGENTS 契約與回歸測試，本批未重建 `dist`；Vue wrapper 維持原狀。
- 新增 FabGrid Column `allowSorting` option，預設為 `true`；設為 `false` 時，滑鼠或 `toggleSort()` 都不會改變排序、不觸發排序事件，也不會在 `remote: true` 時送出查詢。另確認 `sortingColumn` option／event handler 回傳 `false` 時同樣取消本機與遠端排序；四種 local／remote 組合均有回歸測試。同步更新 source-mode cache version、README、API 文件、AGENTS 契約，本批未重建 `dist`。
- 完成 `build lite min`：將 Column `allowSorting: false` 與可取消的 `sortingColumn` 本機／遠端排序規則編入 Lite；只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，版本為 `2026.7.23`，確認無 Lite 非壓縮檔或 ESM 發佈檔，Lite／build scope 測試 9/9 通過。
- 補強 `sortingColumn` constructor option：改由排序流程直接呼叫，不再依賴先註冊成 Wijmo-compatible event handler；回傳 `false` 時，本機與遠端都取消排序，其他已註冊的 `sortingColumn` handlers 仍會收到一次 `e.cancel === true`。以實際 Lite min 與 source-mode Header 點擊流程驗證，並補齊 option／event 不重複觸發測試；本批未重建 `dist`。
- 依序完成 `build clear` 與 `build lite min`：先清空 `dist/` 並保留目錄，再將直接取消 `sortingColumn` 的最新修正編入 Lite。最終只保留 `fabui.lite.min.js`、`fabui.lite.min.css` 與 10 個必要圖片，版本為 `2026.7.23`；確認無非壓縮 Lite／ESM 檔、JavaScript 語法有效，Grid／Lite／build scope 測試 93/93 通過。
- 擴充 `unselectRow(row?)` 支援單選模式：可取消目前 selected row，成功後 `selectedRows`／`selectedItems` 回傳空陣列、列選取背景消失，並在排序、篩選與 refresh 後維持未選取；省略 `row` 時取消目前列，指定非目前列或已無 selected row 時回傳 `false`。Active cell 不納入此 API 契約；同步更新 source cache version、README、API 文件、AGENTS 與回歸測試，本批未重建 `dist`。
- 完成 `build lite min`：將單選模式 `unselectRow(row?)` 編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；版本為 `2026.7.23`，確認無非壓縮 Lite／ESM 檔、JavaScript 語法有效，Grid／Lite／build scope 測試 94/94 通過。
- 新增不可取消的公開事件 `selectedRowChanged`，同時支援 constructor option、Wijmo-compatible `addHandler()` 與既有 native emitter。滑鼠／鍵盤／API 換列或取消 selected row 時回傳 `reason: 'selection'`；`setItemsSource()`、遠端資料回傳與 `observeItemsSource` mutation 更新資料來源時，即使 row index 相同也回傳 `reason: 'itemsSource'`。事件參數包含目前與先前的 row index／data item；同列只換 active column 或更新前後皆無 selected row 時不觸發。本批同步更新 source cache version、README、API 文件、AGENTS 與回歸測試，未重建 `dist`。
- 完成 `build lite min`：將 `selectedRowChanged` 公開事件及本輪最新 Grid 修正編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；確認無非壓縮 Lite／ESM 檔，JavaScript 語法有效，Grid／Lite／build scope 測試 97/97 通過。
- 調整 `selectionMode: 'CellRange'` 的互動與視覺：資料 cell 拖曳使用資料列選取背景，active cell 保留一般背景，範圍四周外框共用 `activeCellBorder`；列號點擊會選取該列所有可見欄位，沿列號拖曳可選取連續整列範圍。同步補齊 source cache version、README、API 文件、AGENTS 契約與回歸測試，並以 source-mode 實際拖曳驗證；本批未重建 `dist`。
- 完成 `build lite min`：將最新 CellRange 整列範圍選取與共用 `activeCellBorder` 外框編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；版本為 `2026.7.23`，確認無非壓縮 Lite／ESM 檔，JavaScript 語法有效，Lite／build scope 測試 9/9 通過。
- 修正 CellRange active cell 視覺：多格範圍的 active cell 維持一般背景，並補齊與單格選取相同的四邊框；與範圍外框重疊的邊由既有 perimeter border 繪製，其餘邊由 active overlay 補齊，粗細仍完全沿用 `activeCellBorder`。同步更新 source cache version、API 文件、AGENTS 契約與 CSS 回歸測試；本批未重建 `dist`。
- 新增獨立附加元件 `fabui.HtmlEditor`：使用 pure JavaScript 實作 Summernote 風格分組工具列、文字／段落／字型／色彩、清單、表格、連結、Data URL／外部圖片、影片、水平線、undo／redo、全螢幕、HTML 原始碼、拖曳高度與 textarea 表單同步；對話框重用既有 Button、Window、EditBox、CheckBox 與 FileBox，並提供三語系、19 組 theme metadata、獨立 source／build 入口、Demo、API 文件與回歸測試。本批未重建 `dist`。
- 將 `fabui.HtmlEditor` 工具列的 Unicode 近似字元改為 Summernote 風格的專用細線圖示，涵蓋樣式、文字格式、清單、段落、表格、連結、圖片、影片、歷史、全螢幕、原始碼與說明；第一顆樣式按鈕直接採用 Summernote `note-icon-magic` 官方字型的實際向量輪廓，超連結改用加寬、加粗的水平雙鏈環與中央連接段。圖示集中於 `src/fabui.icon.css`，並由獨立 HtmlEditor build 擷取到自己的 CSS。以 Default／Black theme 驗證圖示換色、連結對話框與選單互動，完整測試 531/531 通過；本批未重建 `dist`。
- 移除 `fabui.HtmlEditor` focused 狀態的外框高亮與額外光暈；編輯區取得焦點時維持原本邊框顏色及粗細，工具列按鈕與 resize handle 的鍵盤 `focus-visible` 提示不受影響。本批未重建 `dist`。
- 補齊 Codex build 命令 `build htmleditor min` 的明確契約，固定對應 `npm run build:htmleditor -- min`，只產生並保留 `dist/fabui.htmleditor.min.js` 與 `dist/fabui.htmleditor.min.css`，不重建 core 或其他獨立 bundle；同步加入 README 與 build scope 回歸測試，本批未重建 `dist`。
- 完成 `build htmleditor min`：只產生並保留 `dist/fabui.htmleditor.min.js` 與 `dist/fabui.htmleditor.min.css`，已包含新版工具列圖示與 focused 外框規格；確認無 HtmlEditor 非壓縮檔或 ESM 發佈檔、JavaScript 語法有效，HtmlEditor／build scope 測試 15/15 通過。
- 調整 `fabui.HtmlEditor` 編號清單工具列圖示：移除過小的 SVG 數字路徑，改用放大的 `1／2` 數字欄與兩條對齊文字線，圖示加寬至 18px 並維持 theme `currentColor`。本批未重建 `dist`。
- 放大 `fabui.HtmlEditor` HTML 原始碼工具列圖示：`</>` 字形由 11px 提升至 14px、圖示寬度由 22px 增至 24px，字重調整為較細的 400，維持 30px 按鈕高度與水平／垂直置中。整體沿用 theme `currentColor`，本批未重建 `dist`。
- 在 `fabui.HtmlEditor`「文字顏色」右側新增獨立「文字底色」按鈕與 `backcolor` toolbar action；兩者共用色票與 popup lifecycle，分別執行 `foreColor` 與 `backColor`，並補齊 `en`、`zh-TW`、`zh-CN` locale。文字底色圖示使用黃色底色的 `A` 與獨立下拉箭頭，本批未重建 `dist`。
- 依使用者提供的 Summernote 色盤 HTML 與圖片，將 HtmlEditor 共用色票改為固定 8×8、共 64 個精確色碼；色塊使用 20×20px 無間距排列，順序由第一列灰階、第二列高彩度至最後一列深色完全對應參考。移除參考色盤不存在的第 65 顆清除色彩按鈕，清除格式仍由既有工具列按鈕處理；本批未重建 `dist`。
- 在 HtmlEditor 8×8 色盤下方重新加入原本的紅色斜線「清除顏色」按鈕；按鈕跨越 8 欄並水平置中，不改變 64 色的排列與順序，文字顏色及文字底色色盤共用相同行為。同步補齊三語系 label 與 API 文件，本批未重建 `dist`。
- 將 HtmlEditor 色盤下方獨立的清除顏色列移除，改由紅色斜線「清除顏色」按鈕直接取代 8×8 色盤的右下角色塊；文字顏色與文字底色色盤維持相同排列與行為，本批未重建 `dist`。
- 依參考圖重畫 HtmlEditor 第一顆「樣式」按鈕：改用實心斜向魔術棒、分段棒頭與三組四向閃光，不再使用線框近似圖形；維持 16px 圖示、30px 按鈕與右側下拉箭頭。本批未重建 `dist`。
- 完成 `build lite min`：將 CellRange active cell 的完整四邊框修正編入 Lite，只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片；版本為 `2026.7.23`，確認壓縮 CSS 已包含 active overlay、無非壓縮 Lite／ESM 檔，JavaScript 語法有效，Lite／build scope 測試 9/9 通過。
- 補齊 CellRange 的 19 主題契約：Default 與其他 18 組外部主題都明確定義 `--fg-range-selected-bg`、`--fg-range-active-bg`、`--fg-range-border`，讓拖曳範圍背景、active cell 背景及四邊框分別跟隨目前主題，同時維持 `activeCellBorder` 控制粗細；新增全主題回歸測試並同步更新 source cache version、API 文件與 AGENTS 契約。
- 完成 `build theme min`：產生 18 份外部主題壓縮 CSS，全部包含 CellRange 三個主題語意變數；確認無非壓縮主題 CSS、無 ESM 發佈檔，並保留 Mono 72 個 SVG 資產。主題／build scope 測試 14/14 通過，未重新編譯 FabUI、Lite、Diagram、Gantt 或 Scheduler bundle。
- 在 `demo/dev-grid.html` 頂部工具列新增「選範圍」開關，關閉時使用 `selectionMode: 'Cell'`，開啟時即時切換為 `selectionMode: 'CellRange'`；狀態沿用 Demo 本機設定，並補齊 `en`、`zh-TW`、`zh-CN` 文字與回歸測試。此開關只顯示於 source-mode Grid Demo，正式版 Grid Demo 維持既有 `Cell` 行為；本批未重建 `dist`。
- 將 `demo/dev-grid.html` 工具列「凍結欄」與「右凍結欄」的 Number EditBox 都設為 `spinner: true`，提供右側數值增減按鈕。同步更新 source-mode cache version 與 Demo 回歸測試，本批未重建 `dist`。
- 將 `demo/grid.html` 同步到左右凍結欄 Number EditBox 的 `spinner: true` 設定，更新 build-mode Demo 的 `grid2-components.js` cache version；目前 `dist/fabui.min.js` 因既有 `build clear`／Lite min 工作狀態不存在，本批依指示不重建 `dist`，以靜態與非 build 測試驗證 Demo 配置。
- 將 `fabui.HtmlEditor` 在 `demo/dev.html` 移到 `Windows 7 Desktop` 正後方，並同步調整 `demo/index.html` 的正式版排列順序；其他 Demo 項目、標籤與連結維持不變。
- 完成 `build all`：依序編譯 FabUI core、Lite、Diagram、Gantt、Scheduler 與 HtmlEditor，全部產生一般版及壓縮版 JS／CSS，版本為 `2026.7.23`；外部主題輸出 18 組一般版與 18 組壓縮版 CSS，Mono 保留 72 個 SVG，確認沒有任何 ESM 發佈檔。完整測試 533/533、core／Gantt／Scheduler browser smoke 與 JS 語法檢查均通過。另修正 core smoke 的合法 standalone bundle 清單，加入 `fabui.htmleditor.*`，避免正確的 HtmlEditor 四個輸出被誤判為 unexpected。
- 將 `demo/grid.html` 的功能配置與 `demo/dev-grid.html` 完整同步：正式版新增位於「多選」後方的「選範圍」開關，沿用 `Cell`／`CellRange` 切換與本機設定，左右凍結欄皆保留 Number EditBox spinner；同步 toolbar、data、locale、Grid 與 Spinner component query version，並以 README、AGENTS 與回歸測試固定兩頁只允許 source／dist 及必要 locale 載入差異。完整測試 533/533、正式版實際互動及 console 檢查均通過。
- 在 `demo/dev-editbox.html` 新增兩組 source-only 自訂按鈕範例：第一組使用兩個右側按鈕執行搜尋填值與清除，第二組使用 `align: 'left'` 顯示左側參照按鈕；同步 Demo 輸出、cache query version 與回歸測試，正式版 `demo/editbox.html` 維持不變。
- 將 `demo/editbox.html` 按照 `demo/dev-editbox.html` 同步：正式版補上兩個自訂按鈕、左側自訂按鈕、三種 Number Spinner、時間與時間 Spinner 範例，並同步欄位版面、FabUI Button 標記與共用 Demo script cache version；兩頁只保留正式版／開發版標題及 dist／src 載入差異，本批未重建 `dist`。
