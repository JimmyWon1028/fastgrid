## 完成進度

- 完成 2026-07-18 正式 build：重新產生 FabUI core、Vue 2 wrapper、jQuery wrapper、EditBox jQuery wrapper、16 組 theme 與 build-mode Demo；`fabui.version` 更新為 `2026.7.18`，確認六個 FabUI 主發佈檔存在且獨立 `dist/editbox.*` 維持移除。`npm test` 285／285 通過、完整 smoke test 全部通過，並通過 `git diff --check`。
- 修正 `fabui.SplitButton` 從右側箭頭移入 popup 時誤關閉：MenuButton trigger 與 popup 的 hover lifecycle 改用同一 mouse／pointer event family，popup 進入事件會在 trigger 離開事件之後正確取消隱藏計時。
- 修正 `fabui.MenuButton` 開發版視覺：icon、文字與下拉箭頭改為無分隔線的連續排列，並縮小箭頭占位以貼近 EasyUI Default；`fabui.SplitButton` 的獨立箭頭操作區仍保留，但分隔線預設透明，只在滑鼠移入整個按鈕時顯示。
- 完成 `fabui.Tree`：參考 EasyUI Tree／Default theme，提供 nested markup／data、展開收合動畫、checkbox cascade、lines、`iconCls`、完整動態節點 API、右鍵事件、半透明插入線拖放、inline edit、filter、callback／Promise lazy loading、鍵盤、ARIA、三語系、16 組 theme、Control lifecycle、source／build Demo、API、單元測試與 build／smoke wiring。
- 完成 `fabui.SplitButton`：參考 EasyUI SplitButton／Default theme，直接組合既有 `fabui.MenuButton`，主區域只執行主動作，右側箭頭區 click／hover 才展開 Menu；支援 disabled、Menu 對齊、鍵盤、ARIA、16 組 theme、Control lifecycle、source／build Demo、API、測試與 build wiring。
- 完成 `fabui.Messager`：參考 EasyUI Messager／Default theme，以 singleton 提供 show／alert／confirm／prompt／progress，重用既有 `fabui.Window` 與 `fabui.Button`，支援非同步 callback、Toast 堆疊、Progress bar handle、三語系、鍵盤、ARIA、16 組 theme、source／build Demo、API、測試與 build wiring；Dialog／Toast／Progress 關閉後會同步清除暫存 host，避免殘留內容或 listener。
- 完成 `fabui.MenuButton`：參考 EasyUI MenuButton／Default theme，直接組合既有 `fabui.Button` 與 `fabui.Menu`，提供 selector／element／Menu instance、hover／click、左右對齊、下拉箭頭、disabled、鍵盤、ARIA、16 組 theme、Control lifecycle、source／build Demo、API、測試與 build wiring。
- 完成 `fabui.Menu`：參考 EasyUI Menu／Default theme，提供 context／inline menu、巢狀 submenu、自訂內容、icon、separator、disabled、完整 runtime item API、三語系 ARIA、鍵盤、viewport 翻轉、Escape／外部點擊關閉、16 組 theme、Control lifecycle、source／build Demo、API、測試與 build wiring。
- 為 `fabui.Tabs` 新增 `draggable` 開關（預設 `false`）；啟用後可用滑鼠左右拖曳 `top`／`bottom` 頁籤排序，保留選取 panel、支援 overflow 邊緣捲動；跟隨游標的 drag image 使用 50% 透明背景、文字與 icon，保留清楚的 theme 外框與陰影，原位置 Tab 保持原樣且目的地仍可透視；往左拖時在目標 Tab 左側顯示插入線並插到左方，往右拖時在目標 Tab 右側顯示插入線並插到右方。55% 半透明提示線不被 overflow 裁切、不顯示箭頭，並依 16 組 theme 使用主色補色。
- 修正 `fabui.Tabs` overflow 左右捲動按鈕同時繪製 sprite 與文字箭頭造成的 icon 重疊，統一只保留 theme sprite，並避免 hover 清除 icon。
- 為 `fabui.Panel` 的最大化／還原、收合／展開加入預設 180ms 過渡動畫；支援 `animate`、`animationDuration`、快速狀態切換清理、動態尺寸與 `prefers-reduced-motion`，並依 `halign` 對上方標題過渡高度、左／右標題過渡寬度。
- 完成交付前稽核與重構：DatePopup／ComboPopup／ColorPopup 的全域 listener 改為只在開啟期間存在；修正 Tabs destroy 還原初始 markup、Panel／Window dispose 還原原始屬性、Layout 缺少 center 的半初始化殘留與動態 region 清理、Window 在極窄 viewport 的 constrain，以及 smoke 漏驗 Panel／Layout／Window；build scripts 同步支援隔離輸出目錄以便不改寫工作區 `dist` 進行完整驗證。
- 統一 FabGrid 欄位、Grid／TreeGrid 資料列與 PivotPanel 欄位拖曳的插入指示線為 55% 半透明，並同步正式 Demo 覆寫、提示圖例與 source-mode 快取版本。
- 修正 Grid／TreeGrid 資料列拖曳插入指示線寬度，右邊界依實際欄位總寬與可用 viewport 取較小值，不再延伸到右側空白或垂直捲軸。
- EditBox 的公開使用方式統一為 `new fabui.EditBox(...)`，並納入 FabUI core 的 JavaScript、ES module 與 CSS bundle；正式與 source-mode Demo 改為直接載入主 `fabui`，移除獨立 EditBox build 與 `dist/editbox.*`，並同步更新 build、smoke、API、README、TODO 與產品契約。
- 稽核目前已交付的 FabGrid、EditBox、Chart、Pivot 與 Vue／jQuery wrapper，補齊 API 手冊中的公開 namespace、建構選項、方法、事件、popup 行為、進階 Pivot 計算與 wrapper 對應說明，並修正過時的 Grid 右鍵選單與 EditBox 發佈方式。
- 抽出 `src/editbox/date-popup.js` 作為 FabGrid 與 `fabui.EditBox` 共用的內部 DatePopup；Calendar DOM、月份選單、鍵盤與關閉 lifecycle 統一採用 EditBox 的 `.fui-calendar-*` 實作，移除 Grid 重複的 `fg-datebox-*` renderer 與 CSS，Grid 僅保留 cell／Search Row 的日期寫回邏輯。
- 抽出 `src/editbox/color-popup.js` 作為 FabGrid 與 `fabui.EditBox` 共用的內部 ColorPopup；色票、HSV、Alpha、pointer drag 與關閉 lifecycle 統一採用 EditBox 的 `.fui-colorbox-*` 實作，移除 Grid 重複的 `fg-color-*` popup renderer、顏色轉換工具與 CSS，並讓 Grid 自訂色票保留 CSS 顏色名稱原文。
- 抽出 `src/editbox/combo-popup.js` 作為 FabGrid 與 `fabui.EditBox` 共用的內部 ComboPopup；option、group、active／selected 狀態、鍵盤導覽、寬度量測與關閉 lifecycle 統一採用 EditBox 的 `.fui-combobox-*` 實作，移除 Grid 重複的 `fg-combobox-*` popup renderer、事件與 CSS，Grid 僅保留 cell／Search Row 的資料過濾、驗證與值寫回邏輯。
- 新增 `src/editbox/editor-icons.js` 統一 FabGrid cell editor、Search Row 與 `fabui.EditBox` 的 icon descriptor；公開寫法收斂為 `iconCls`、`title`、`ariaLabel`、`text`、`width`、`align`、`keepFocus`、`onClick`，舊欄位名稱只在共用正規化入口保留相容。
- 共用 DatePopup 新增預設關閉的 `showLunar`；開啟後以雙行日期格顯示國曆與農曆日期，初一包含農曆月份，並由 `fabui.EditBox`、FabGrid cell editor 與 Search Row 共用同一套 option、轉換與樣式。
- EditBox 與 Grid Demo 移除主動設定 `showLunar: true`，讓範例與正式 API 一致呈現農民曆預設不顯示；需要時仍可明確開啟。
- DatePopup 新增 Calendar theme 契約，依 `res/themes/*/calendar.css` 整理 16 組獨立 CSS variables；Date EditBox 預設繼承外層 `fg-theme-*` class 並可用 `theme`／`setTheme()` 覆寫，FabGrid popup 會自動跟隨 root theme，且 source 與 build 不依賴 `res/`。
- 完成 `fabui.Window`：依 EasyUI Window／Material Teal 契約實作 pure JavaScript 拖曳、八方向縮放、modal mask、置中、收合、最小化、最大化／還原、16 組 theme、三語系工具、Control registry 與可取消 lifecycle；接入 FabUI 主入口、build、Demo、API 與測試。
- 完成 `fabui.Panel`：依 EasyUI Panel／Material Teal 契約實作 header／body／footer、custom tools、收合、最小化、最大化／還原、關閉、fit、垂直標題、遠端 lazy load、cache、16 組 theme、三語系、Control registry 與可取消 lifecycle；接入 FabUI 主入口、build、Demo、API 與測試。
- 完成 `fabui.Layout`：north／south／east／west／center 五區重用 `fabui.Panel`，實作 dock geometry、collapsed bar、float／dock expand、pointer／keyboard Splitter、動態 add／remove、split／unsplit、巢狀 resize、16 組 theme、三語系與 lifecycle；接入 FabUI 主入口、build、Demo、API 與測試。
- 調整 `fabui.Window`：右上角預設只顯示最大化／還原與關閉，最大化／還原、最小化／重新開啟、收合／展開加入過渡動畫；左上角標題 icon 統一使用 `iconCls: 'icon-xxx'`，新增共用 `.icon-window`。
- 修正 Layout Demo 的 viewport 高度：以外層 `.layout-stage` 承擔頁面留白，避免 `fit: true` 的 100% host height 再加 bottom margin，確保 south region 完整位於螢幕內。
- 為 `fabui.Layout` 的 edge region 收起／展開加入 180ms 幾何過渡，同步動畫 center、Splitter 與 collapsed bar；支援 `animate`／`animationDuration`，並遵守 `prefers-reduced-motion`。
- 修正 `fabui.Button` icon DOM 排列順序，使預設 `iconAlign: 'left'` 實際顯示在文字左側，並讓 right／top／bottom 四方向與 API 名稱一致。
- 統一所有 source-mode 與 build-mode Demo 的初始 theme 為 `default`：每個 `demo/*.html` body 明確使用 `fg-theme-default`，所有主題選單明確選取 default，並新增自動掃描測試避免預設主題漂移。
- 完成 `fabui.Button`：依 EasyUI LinkButton／Default theme 契約實作 pure JavaScript `<a>`／`<button>`、尺寸、disabled、plain、outline、toggle、selected、group、四方向 icon、small／large、16 組 theme、Control registry 與 dispose 還原；接入 FabUI 主入口、build、Demo、API、TODO 與測試。
- 完成 `fabui.Calendar`：依 EasyUI Calendar／Default 契約實作 pure JavaScript 年月導覽、月份選單、週次、formatter、styler、validator、三語系、16 組 theme、鍵盤、Control registry 與 dispose 還原；以嵌入模式共用既有 DatePopup renderer 與 EditBox Calendar 樣式，農民曆預設關閉，並接入 FabUI 主入口、build、Demo、API、TODO 與測試。
- 完成 `fabui.Tabs`：擴充既有未公開 Tabs 核心，依 EasyUI Tabs／Default 契約補齊 markup／programmatic panels、動態新增／插入／更新／關閉、tab tools、header tools、四方向排列、overflow scroller、遠端內容、disabled、keyboard、三語系、16 組 theme、Control registry 與 dispose；接入 FabUI 主入口、build、Demo、API、TODO、單元測試與 smoke。
- 完成 `fabui.Tooltip`：依 EasyUI Tooltip／Default 契約實作 pure JavaScript 四方向定位、HTML／function content、滑鼠追蹤、延遲、viewport 翻轉、Escape、外部點擊、互斥 popup、16 組 theme、Control registry 與 dispose；接入 FabUI 主入口、build、Demo、API、TODO、單元測試與 smoke。
