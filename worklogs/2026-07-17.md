# 2026-07-17

## 完成進度

- PivotGrid 列欄位 Header 的排序符號改為沿用 FabGrid 的升冪／降冪三角形，並依 `PivotField.descending` 顯示目前排序方向。
- 移除 PivotGrid 原本固定顯示的 `↕` 字元，補上排序符號 DOM 契約測試與 source-mode cache version。
- 修正 Grid 在展開狀態捲到最右側後縮減欄位時，scroll-linked Header 沿用舊最大捲動距離而移出畫面的問題；layout 更新後於下一個 animation frame 合併校正 Header／Footer 的最終同步距離。
- PivotGrid 疊合欄改為只在畫面隱藏而不從完整 columns 集合移除；預設 Excel 匯出保留全部疊合子欄與資料，並將這些欄位維持為 hidden column。
- PivotGrid Excel 匯出改用完整 Pivot rows；畫面疊合後未顯示的業務明細列仍寫入工作表，並依目前疊合狀態維持為 hidden row。
- PivotGrid 列欄位 Header 排序改為與 FabGrid 相同的預設／升冪／降冪三態循環；預設不顯示排序符號並保留 dimension 原始出現順序，右鍵選單同步提供清除排序。
- PivotPanel 的 Rows／Columns 欄位新增右鍵排序 popup，可直接設定預設順序、升冪或降冪；選項使用 radio active 狀態並支援 `Escape` 與點擊外部關閉。
- PivotGrid Header Cell 右鍵選單新增 Grid 全螢幕項目，沿用 FabGrid `toggleFullscreen()`、既有 icon 與 locale，並依目前狀態切換進入／離開文字。
- 新增 `fabui.pivot.PivotChart`，直接監聽共用 PivotEngine 的 `updatedView`，將 leaf row／column aggregates 轉為既有 SVG Chart 的 categories／series；支援 Column、Bar、Line、Pie、圖例、Pie series、三語 locale、顯示上限與 Control lifecycle。
- `demo/dev-pivot.html` 改為桌面三欄 PivotPanel／PivotGrid／PivotChart 工作區，加入圖表類型、圖例、Pie series 與隱藏／開啟圖表控制；窄畫面維持單頁 RWD 上下排列。
- 修正 `fabui.Chart` 公開 `legend`／`footer` option property 與內部 DOM 使用同名 reference 的衝突；內部改用 `legendElement`／`footerElement`，避免 PivotChart 設定圖例或 footer 時覆蓋 DOM。
- Chart Cartesian X 軸依可用寬度自動降低分類標籤密度並加寬 Y 軸左側邊距；PivotChart 預設使用 locale 千分位數字格式，改善右側窄 Panel 的刻度可讀性。
- 重製 Pivot Demo 為固定 144 筆差異化資料，將預設圖表收斂為 4 季、3 產品與單一銷售額指標；產品規模及季度趨勢採明顯級距，並同步精簡平台、區域與月份 View。
- PivotChart 的 Pie 預設啟用 Point selection 與扇形內百分比標籤；點擊扇形會直接沿用 `fabui.Chart` 的選取旋轉及位移動畫，仍可由 `selectionMode: 'None'` 或 `dataLabel: null` 關閉。
- PivotChart 新增 PivotGrid `selectionSource` 雙向連動，圖形 point 與 Grid 彙總 cell 依 row key、data-column binding 精確對應；Chart 點擊會選取並捲動 Grid，Grid 點擊會反向標記圖形，Pie 會同步切換系列。
- PivotChart 綁定 PivotGrid 後改為監聽 Grid `refreshed`，依目前疊合／展開狀態重建圖表；展開群組顯示 leaf aggregates，疊合群組以可見 subtotal 取代隱藏明細，並維持圖表與 Grid 雙向選取，不重新彙總原始資料。
- `demo/dev-pivot.html` 的圖表類型選擇加入獨立 Demo locale pack；繁中顯示直條圖／橫條圖／折線圖／圓餅圖，簡中顯示柱状图／条形图／折线图／饼图，英文維持 Column／Bar／Line／Pie，切換語系時同步更新選項與 aria-label。
- 移除 `demo/dev-pivot.html` 與 `demo/pivot.html` PivotGrid 標題列的「全部疊合／全部展開」按鈕及狀態同步程式；個別 `＋／−` 與列欄位右鍵選單的全部群組操作維持不變。
- 新增 `fabui.pivot.PivotWorkspace` 三合一組合元件，直接重用 PivotPanel／PivotGrid／PivotChart 並共用單一 PivotEngine；Auto layout 依 host 寬度切換三欄／三列，兩條 Splitter 支援 pointer drag、方向鍵、尺寸限制與拖曳期間 document listener lifecycle，並公開 `engine`、`panel`、`grid`、`chart`。
- 新增 `demo/dev-pivot-workspace.html` source-mode 與 `demo/pivot-workspace.html` build-mode Demo、三語 Workspace 標題／Splitter aria 文字、`docs/pivotworkspace-api.md`、公開入口、bundle build 配置及尺寸計算測試。
- Pivot Demo 的第一個 View 改為「季度／區域／產品（銷售額）」，Rows 使用季度與區域兩層結構並產生季度小計，確保進入 Demo 即可操作 PivotGrid 的展開／疊合。
- PivotGrid Row field 右鍵選單新增單一「全部展開／全部疊合」狀態項目；一次操作所有列與欄群組，仍有任一展開群組時顯示全部疊合，全部疊合後交換為全部展開，且不顯示單一「展開群組／疊合群組」項目。
- PivotGrid Row field 右鍵 popup 補上點擊外部立即關閉；popup 內點擊維持既有項目操作，並保留 `Escape` 關閉行為。
- 全面盤點 FabGrid、PivotPanel、PivotGrid、standalone DateBox／ComboBox、Pivot 明細與 Demo lookup popup；統一 Grid 右鍵選單、Filter、欄位選擇器及 datebox／combobox／color editor popup 的外部點擊關閉流程，移除跨 popup 判斷的提早中止，確保點進其中一個 popup 時其餘 popup 也會關閉。
- PivotWorkspace 的 PivotGrid 標題列內建定義區／圖表區顯示切換按鈕，PivotChart 標題列內建 Column／Bar／Line／Pie 多語系圖形選單；新增 `setChartType()`／`chartType` 並移除兩個 Workspace Demo 重複維護的控制項。
- PivotWorkspace 的 Grid／Chart 標題列最右側新增 Pane 全螢幕按鈕，以 Fullscreen API 放大包含標題列與內容的完整 Pane；不支援原生 API 時自動使用固定定位 fallback，按鈕支援三語 aria／title、再次點擊或 `Escape` 離開、狀態同步與 Grid／Chart resize。
- PivotWorkspace Horizontal `chartSize` 擴充為 `number | percentage | fr`，預設改為 `'40%'`；百分比／`fr` 以 Grid＋Chart 可分配空間計算，Grid 為隱含 `1fr`，並在使用者拖曳 Chart Splitter 後轉為固定 px。
- 完成 PivotWorkspace 正式 build 與 `demo/pivot-workspace.html`；修正 PivotPanel／PivotChart／PivotWorkspace 私有 helper 在 bundle 展平後的同名衝突，重建六個 `dist/fabui.*` 主檔及 wrapper bundles，並通過 smoke、正式 browser-global、40% Chart、圖形切換與全螢幕驗證。
- `demo/pivot-workspace.html` 工具列新增主題、語系與 Pivot View 範例切換；範例共用既有 PivotEngine 並只替換 `viewDefinition`，語系切換同步 Workspace 控件、工具列標籤、選項與統計文字。
- `demo/dev-pivot-workspace.html` 工具列與正式 Demo 對齊，加入相同的主題、排列、語系、Pivot View 範例選項與三語 Demo 文字同步。
- README 線上 Demo 清單新增 `demo/pivot-workspace.html` 的 GitHub Pages 連結。
- 重構 Pivot 共用 typed-value／path 工具，統一 Engine filter、Grid 收合與 Chart 可見階層的 Date／NaN 比較；Grid 收合判斷改為直接查詢 ancestor path key，避免逐列掃描全部收合群組。
- 修正 PivotEngine `setFields()` 後四個 View 區域仍引用舊 PivotField instance、Average 將無效數值算入分母，以及 Date filter 經 JSON viewDefinition 還原後無法命中的問題。
- PivotGrid 在 Pivot view 更新後改用 row key 與 data-column key 保留邏輯選取並安全 clamp；PivotChart 重繪後重新同步 Grid 選取，Grid 右鍵 aggregate 與 row field 排序同步觸發 `viewDefinitionChanged`。
- PivotPanel 的 document 外部點擊 listener 改為只在 aggregate／sort popup 開啟期間綁定，popup 關閉或 dispose 立即解除；popup 標題分隔線改用 `--fg-border` theme variable，並移除 PivotWorkspace 未使用的 engine ownership 狀態。
- PivotGrid 語系切換改為先以 silent mode 重建 Pivot 欄列，再交由 FabGrid locale 流程單次 render，移除原本連續兩次完整繪製。
- TreeGrid 樹欄所有資料 cell 新增核心右鍵功能表，沿用 FabGrid 既有 popup 容器、樣式、`Escape` 與外部點擊關閉流程；功能表以單一狀態項目切換整棵樹的全部展開／全部疊合。
- TreeGrid 右鍵功能由 `demo/js/treegrid.js` 移入 `src/grid/fabgrid-tree.js`，補齊 `en`、`zh-TW`、`zh-CN` locale、核心契約測試與 source-mode cache version，Demo 只保留初始化及狀態顯示。
- 完成 TreeGrid source-mode 瀏覽器互動驗證：根節點與葉節點右鍵皆顯示單一狀態項目；全部疊合後可視節點由 18 筆降為 3 筆，再切回全部展開；`Escape`、點擊外部可關閉，非樹欄資料格不會誤開啟，瀏覽器無錯誤或警告。
- 整理 `TODO.md` 已過期的模組拆分清單，補列已完成的 editor runtime、filter UI、selection、drag、view 與相容類型模組，並將驗證方式改為持續開發規則。
- TreeGrid 與文件批次通過 `npm test` 共 172 項、相關 JavaScript 語法檢查及 `git diff --check`。
- PivotEngine 新增可取消的 `refreshAsync({ batchSize })`、進度事件、`cancelRefresh()` 與非同步 `deferUpdate()`；同步 `refresh()` 契約維持不變，取消時保留上一份完整 Pivot view。
- PivotField 新增 calculated field、WeightedAverage 權重欄位與 `PivotShowAs`；支援總計／列總計／欄總計百分比、與前一項差異、差異百分比及累計，並同步 viewDefinition 與 API 文件。
- PivotPanel 新增多值 filter 草稿 dialog、搜尋、全選、套用／取消、`Escape`／外部點擊關閉與滑鼠／觸控拖放；Values 右鍵選單同步支援 WeightedAverage 與 ShowAs。
- 新增 `fabui.pivot.PivotSlicer` source 元件，支援搜尋、多選草稿、延後套用、清除、共用 PivotField filter、三語 locale、theme CSS 與 lifecycle。
- PivotWorkspace Grid 標題列新增非同步進度、取消及錯誤狀態，並公開 `refreshAsync()`／`cancelRefresh()`；新增 `demo/dev-pivot-advanced.html` 展示 12,000 筆資料、Slicer、calculated field 與 WeightedAverage。
- Vue 2 wrapper 新增 `FabPivotPanel`、`FabPivotGrid`、`FabPivotChart`、`FabPivotWorkspace`、`FabPivotSlicer`；jQuery wrapper 新增對應的五個薄封裝 plugin，皆只對應既有 core lifecycle／options／methods，不複製彙總邏輯。
- 依產品決策將 Server-side Pivot／OLAP／SSAS 與 standalone TextBox／NumberBox／DateBox／ComboBox／Tabs 排除目前 roadmap，並同步 README、TODO、AGENTS 與 Pivot／wrapper API 文件。
- 完成 source-mode 瀏覽器驗證：PivotPanel filter 草稿按 `Escape` 不套用、確認後更新 Grid，PivotSlicer 延後套用可移除指定區域，Workspace 顯示進度、可取消並可重新完成；瀏覽器無錯誤或警告。
- Pivot 進階批次通過 `npm test` 共 182 項、相關 JavaScript 語法檢查及 `git diff --check`。
- 依使用者明確要求完成正式 build；補齊 browser-global／ESM bundle 的 `PivotSlicer` 與 `PivotShowAs` 組裝，重建六個 `dist/fabui.*` 主檔及 Vue 2／jQuery wrapper bundles，完整 `npm run smoke` 通過。
- 新增 `demo/index.html` Demo 索引，集中列出 README 引用的正式／wrapper Demo 與全部 `demo/dev-*.html` source-mode 開發頁面；README 同步加入索引入口。
