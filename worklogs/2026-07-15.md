# 2026-07-15 工作紀錄

## 完成進度

- 檢查 FabGrid 核心模組現況、工作樹與測試契約，確認下一個完整責任領域應先完成 data／grouping，而 editor 應保留為下一批獨立拆分。
- 將 row groups 設定、grouped view 建立、group state、group row／footer 判斷、aggregate 協調與群組收合流程移入 `src/grid/fabgrid-data.js` installer，公開 API 與既有行為維持不變。
- 新增 data installer 的 grouping／aggregate 協調測試，避免只測 helper 而漏掉 prototype 安裝邊界。
- 同步更新 `src/fabui.js` 與 source-mode Demo query version，避免瀏覽器沿用拆分前的核心模組快取。
- 本批通過 JavaScript 語法檢查、`git diff --check`、43 項單元測試，以及 `/tmp` 隔離副本完整 build／smoke；工作區 `dist` 未改寫。
- 啟動 source-mode 開發伺服器並確認 `demo/dev-grid.html` 回應 HTTP 200。
- 將 Pure JavaScript source-mode Demo 入口由 `demo/dev.html` 更名為 `demo/dev-grid.html`，並同步更新所有專案內引用。
- 將 Vue 2 Demo HTML 入口由 `demo/demo-vue2.html` 更名為 `demo/grid-vue2.html`，並同步更新專案規則與 Vue API 文件網址。
- 將 jQuery build-mode Demo 入口由 `demo/demo-jquery.html` 更名為 `demo/grid-jquery.html`，並同步更新 README 與專案規則引用。
- 將 Pure JavaScript build-mode Demo 入口由 `demo/index.html` 更名為 `demo/grid.html`，並同步更新 README、專案規則與歷史文件引用。
- 統一 `demo/js/` 功能命名：一般 Grid 改用 `grid.js`、`grid-data.js`、`grid-locales.js`、`grid-query.js`、`grid-toolbar.js`、`grid-jquery.js`、`grid-vue2.vue` 與 `grid-vue2-systemjs.config.js`；TreeGrid 使用 `treegrid.js` 與 `treegrid-data.js`。第三方 `system.js` runtime 保留原名。
- TreeGrid build-mode 與 source-mode Demo 啟用欄位搜尋列，並加入與 Grid Demo 一致的右下角快速搜尋、OR／AND 模式及清除操作；TreeGrid 篩選會保留符合節點的父層路徑。
- TreeGrid 列號改以完整展開後的階層順序為基準；收合節點或套用欄位／快速搜尋時只隱藏列，後續可視節點維持原始列號，`getTreeRow()` descriptor 同步公開 `rowNumber`。
- `demo/dev-treegrid.html` 上方加入搜尋列開關與列號模式選單，可即時切換顯示列號、只保留窄列頭 cell 或完全關閉列頭。
- 修正欄位搜尋列的中文輸入法組字流程：composition 期間取消延遲搜尋且不重畫 header，待 compositionend 後再套用篩選，避免中文尚未完成就失去輸入與篩選狀態。
- TreeGrid Demo 關閉搜尋列時同步清除欄位搜尋條件，避免重新開啟後沿用隱藏的舊條件，造成新輸入組合成 0 筆結果。
- FabGrid 核心左上角列頭 cell 新增右鍵功能表，沿用白底、左 icon 欄分隔線與 32px 列高 popup 規格，提供搜尋列顯示／隱藏、Excel／CSV 匯出及 Grid fullscreen，並補齊 `en`、`zh-TW`、`zh-CN` 文字。
- `setShowSearchRow()` 新增 `searchRowVisibilityChanged` 事件，Grid 與 TreeGrid Demo 的上方搜尋列控制項會在右鍵功能表切換後同步狀態。
- 左上角右鍵功能表加入列號三種顯示選項：關閉、顯示列號、只顯示 cell；目前模式以勾選顯示，並透過 `rowHeaderModeChanged` 同步 Grid／TreeGrid Demo 上方控制項及 wrapper 事件。
- 右鍵功能表的列號模式改為「列號」下層功能表；搜尋列與 fullscreen icon 分別改用共用 `.icon-search`、`.icon-fullscreen`。
- 新增 `src/grid/fabgrid-drag.js` row pointer drag 模組，`allowDragging` 擴充 `'Rows'`／`'All'`，支援一般 Grid 重排、跨 Grid move 與 TreeGrid `before`／`inside`／`after` 落點。
- TreeGrid 新增 `moveTreeItem()`、`insertTreeItem()`、`removeTreeItem()`、階層位置查找與循環防護；節點拖曳可改變同層順序及上下階。
- 新增 source-mode `demo/dev-grid-treegrid.html`、獨立 Demo 資料／JS／CSS；左側為待配置 Grid，右側為組織 TreeGrid，並提供落點圖例、事件狀態、全部展開與重設資料。
- 新增 source-mode `demo/dev-grid-grid.html`、獨立 Demo 資料／JS／CSS；左右兩側皆為一般 Grid，可在各自 Grid 內上下重排，也可跨 Grid 雙向移動，並提供筆數、落點圖例、事件狀態與重設資料。
- Browser 實測 Grid 內向下重排、左至右與右至左跨 Grid 移動均成功；兩側筆數、事件狀態與重設資料正確，拖曳後無可見殘影且 console 無錯誤。
- 依使用者指示執行正式 build；修正 `.icon-remove` 對已更名 `edit_remove.png` 的資產引用，並將 `src/grid/fabgrid-drag.js` 納入 browser global／ESM 主 bundle。
- Build 驗證新增 TreeGrid 與 row drag installer 存在檢查，smoke 的 custom editor icon 斷言同步新資產檔名；六個 FabUI 主檔與 Vue／jQuery wrapper 已重建，54 項單元測試及完整 build-mode smoke 全數通過。
- 新增 build-mode `demo/grid-treegrid.html`，只載入 `dist/fabui.min.js`、`dist/fabui.css` 與既有 Grid／TreeGrid Demo 資料及互動入口，功能與 `demo/dev-grid-treegrid.html` 保持一致。
- Browser 實測 build-mode Grid 拖入 TreeGrid 中央落點成功，待配置筆數由 5 改為 4、樹節點由 11 改為 12；重設後恢復原始資料，無拖曳殘影且 console 無錯誤。
- 新增 build-mode `demo/grid-grid.html`，只載入 `dist/fabui.min.js`、`dist/fabui.css` 與既有 Grid／Grid Demo 資料及互動入口，功能與 `demo/dev-grid-grid.html` 保持一致。
- Browser 實測 build-mode 同 Grid 向下重排及左至右跨 Grid 移動成功；左右筆數由 5／4 改為 4／5，重設後恢復原始資料，無拖曳殘影且 console 無錯誤。
- 新增 `demo/grid-grid-vue2.html` 與 `demo/js/grid-grid-vue2.vue`，沿用 `demo/grid-vue2.html` 的 SystemJS runtime loader 掛載方式，使用 Vue 2 Options API 及 `fabgrid-vue` browser wrapper 建立左右兩個 Grid；Vue 管理資料、筆數、拖曳狀態與重設，core 維持 cell rendering 與 row drag。
- Browser 確認 Vue SFC 由 SystemJS 成功掛載兩個 `.fui-vue-fabgrid` wrapper；修正 SFC 樣式以 HTML 頁面為基準的 import 路徑後，左右版面、同 Grid 重排、跨 Grid 移動、Vue 響應式筆數與重設均正確，console 無錯誤。
- `demo/grid-grid-vue2.html` 上方新增拖曳開關，使用 Vue computed `draggingMode` 與 wrapper `allowDragging` prop 同步切換左右 Grid 的 `Rows`／`None`，並顯示目前狀態。
- Browser 實測初始兩側共 63 個 draggable cells；關閉後降為 0 且跨 Grid 拖曳不改變 5／4 筆資料，重新開啟後恢復 63 個並成功移動為 4／5 筆，console 無錯誤。
- 新增 `demo/grid-treegrid-vue2.html` 與 `demo/js/grid-treegrid-vue2.vue`，沿用 `demo/grid-vue2.html` 的 SystemJS runtime loader 掛載方式，使用既有 Vue 2 wrapper 建立 Grid／TreeGrid component；Vue 管理資料、節點筆數、拖曳狀態、全部展開與重設。
- Browser 確認 Vue 2 SFC 成功掛載兩個 `.fui-vue-fabgrid` wrapper；Grid 拖入 TreeGrid 後筆數由 5／11 改為 4／12，TreeGrid 節點降階後 padding 由 27px 增為 47px，重設恢復原始資料且 console 無錯誤。
- `demo/grid-treegrid-vue2.html` 上方新增拖曳開關，使用 Vue computed `draggingMode` 與 wrapper `allowDragging` prop 同步切換 Grid／TreeGrid 的 `Rows`／`None`，並顯示目前狀態。
- Browser 實測初始 Grid／TreeGrid 共 96 個 draggable cells；關閉後降為 0 且 Grid→TreeGrid 拖曳不改變 5／11 筆資料，重新開啟後恢復 96 個、成功移入為 4／12 筆，TreeGrid 節點降階亦正常且 console 無錯誤。
- Row drag helper 測試加入 before／inside／外部項目與循環階層案例，單元測試增加為 52 項；Browser 實測 Grid 跨入 TreeGrid、TreeGrid 降階與升階均成功。
- 新增欄寬 AutoFit：雙擊 header 欄位分隔線時，依 header、目前 view 的格式化顯示文字、群組 aggregate 與可見 footer 計算合適欄寬，並遵守 `minWidth`。
- 新增公開 `autoSizeColumn(column)` API，以及可取消／調整寬度的 `autoSizingColumn` 和完成後的 `autoSizedColumn` 事件；同步更新 README、AGENTS、API 文件與 source-mode cache version。
- AutoFit 完整 smoke 通過：實際 `dblclick` 將測試欄由 180px 調整為 433px，兩個 AutoFit 事件各觸發一次，後續 virtualization、editor、export 與 wrapper smoke 仍全部通過。
- Browser source-mode 驗證通過：Summary 欄由 240px 縮至 193px，header 與已渲染 cells 寬度一致、抽查 8 個 cells 均未裁切，瀏覽器 console 無錯誤。
- 依 Browser 人工調整結果更新 Demo 預設欄寬：`簡稱` 88px、`日期` 113.5px、`年月` 90px；Pure JS／jQuery 共用 Demo 與 Vue Demo 保持一致，並更新 Demo query version。
- 依 Browser 人工調整結果將 `訂單編號` Demo 預設欄寬更新為 140.4px，同步 Pure JS／jQuery 共用 Demo、Vue Demo 與 query version。
- Grid Demo 移除「幣別」顯示欄，欄位總數由 22 調整為 21；Pure JS、jQuery 與 Vue 2 Demo 欄位定義及 locale pack 同步更新，資料物件的 `crncy` 欄位保持不變。
- README 的線上 Demo 清單新增 Grid / Grid 與 Grid / TreeGrid 拖曳範例連結。
- 參考 `res/TreeGrid` 的 `childItemsPath`、階層收合與 `collapseGroupsToLevel()` 規格，新增 `src/grid/fabgrid-tree.js` TreeGrid 領域模組；樹資料只扁平化為目前可視列，再沿用既有雙向 virtualization renderer。
- TreeGrid 支援節點箭頭、`treeColumn`／`treeIndent`、`getTreeRow()`、`toggleTreeNode()`、`refreshTree()`、全部展開、指定階層收合、左右方向鍵導覽、同層排序，以及篩選時保留祖先路徑。
- 新增獨立 `demo/js/treegrid-data.js` 三層組織資料、共用互動程式與樣式，以及 source-mode `demo/dev-treegrid.html`、build-mode `demo/treegrid.html`。
- 新增 TreeGrid helper／installer 測試；49 項單元測試全數通過。Browser 實測初始 9 列、單一節點展開 12 列、全部收合 3 列、全部展開 18 列，凍結樹欄、節點縮排與鍵盤收合／展開正常。
- `/tmp` 隔離副本完整 build 與既有 browser smoke 通過，並確認 build-mode `demo/treegrid.html` 可由 `dist/fabui.min.js` browser global 建立相同的 9 列初始 TreeGrid；工作區 `dist` 未改寫。
- 依使用者明確指示正式執行 `npm run build`，更新工作區 `dist` 六個 FabUI 主檔、theme 與 Vue／jQuery wrapper；TreeGrid 已納入 browser global／ESM bundle，工作區 browser smoke 通過。
- 本輪發布前再次執行正式 `npm run build`，重建 FabUI 六個主檔、Vue 2／jQuery wrapper 與主題資產；54 項單元測試、完整 browser smoke 及 `git diff --check` 全數通過。
- 新增 16×16 `row-number.png` 正式 theme 資產與 `.icon-row-number`，套用到左上角 popup 的「列號」父選單；下層列號模式與既有行為保持不變。
- 依使用者指示重新執行正式 build，將 `.icon-row-number` 與 `row-number.png` 納入 FabUI CSS、browser global／ESM bundle 及 `dist/theme/images`。
- 左上角 popup 新增 `.icon-clear`「清除篩選」，直接呼叫核心 `clearFilter()`，一次清除 predicate、全域搜尋與所有欄位搜尋；同步補齊 `en`、`zh-TW`、`zh-CN` locale 與 API 文件。
- 新增公開 `setRowHeaderWidth(width)` runtime API，寬度正規化後自動 refresh；jQuery wrapper 的動態 `rowHeaderWidth` option 會優先呼叫正式 setter，並補上核心及 wrapper 測試。
- 依使用者指示正式 build，將左上角 popup「清除篩選」、`setRowHeaderWidth(width)` 與 jQuery setter mapping 納入 browser global、ESM 及 wrapper 發佈檔；56 項單元測試、完整 browser smoke 與 `git diff --check` 全數通過。
- Excel 匯出標題列改為跟隨目前 `headerDisplayMode`；Grid 顯示 binding 時 XLSX 第一列匯出 binding，切回 header 時匯出欄位標題。
- 依使用者指示執行正式 build，將 Excel 標題跟隨 header／binding 顯示模式的邏輯納入 browser global、ESM 與壓縮 bundle；六個 FabUI 主檔、Vue／jQuery wrapper 輸出齊全，57 項單元測試與 `git diff --check` 通過。
- 移除 header、body 與 footer 左右凍結區的邊界陰影；左凍結區右側與右凍結區左側改為不佔 layout 寬度的 2px `--fg-border-strong` 實色分隔線。
- 凍結區 header、body、footer 的共用分隔線調整為 1px，並新增較一般框線更深的 `--fg-frozen-divider: #8f99a8` 獨立主題變數。
- 依畫面效果將 `--fg-frozen-divider` 由 `#8f99a8` 調淺為 `#ccc`，線寬維持 1px。
- 將 `--fg-frozen-divider` 由 `#ccc` 調整為 `#bbb`，線寬維持 1px。
- 將 `--fg-frozen-divider` 由 `#bbb` 調整為中間色 `#c0c0c0`，線寬維持 1px。
- 逐一檢查 16 個內建主題的凍結分隔線；Default／Bootstrap 保留 `#c0c0c0`，Cupertino、Sunny、Pepper Grinder 改用對應色系，Material／Metro 系略深於框線，Dark Hive／Black 改用不刺眼的中間灰；全部維持 1px 與無陰影。
- Source-mode 主題檢查發現巢狀 CSS import 會沿用舊快取；`src/fabui.css` 內部 imports 加入統一 query version，build CSS parser 同步支援忽略 query 後內嵌，不影響正式 bundle 輸出。
- FabGrid `activeCellBorder` 核心預設由 1px 改為 2px，同步 CSS fallback、runtime property fallback、API 文件與單元測試；仍可傳入自訂數值或 `0` 隱藏邊框。
- 依使用者指示執行正式 build，將 16 個主題的 1px 凍結分隔線、2px active cell 核心預設與 Excel header／binding 匯出邏輯納入 browser global、ESM、CSS 及 Vue／jQuery wrapper 輸出；58 項單元測試、六個主檔檢查與 `git diff --check` 通過。
- Body 凍結分隔線由整個 frozen pane 改為實際渲染 cell 邊界；最後一個左凍結 cell、第一個右凍結 cell 與群組列會加入邊界 class，資料列結束後的空白區不再顯示凍結線；header／footer 分隔線維持不變。
- 修正凍結分隔線與 cell 原有 1px dotted border 並排後視覺近似 2px；左凍結邊界改為直接覆寫 `border-right`，右凍結邊界使用單一 `border-left` 並將相鄰 scroll cell 邊框設為透明，header、body、footer 與群組列皆為實際 1px。
- 凍結分隔線顏色統一為 `#ccc`，所有內建主題皆維持相同的單一 1px 邊界。
- 依後續指示保留 Default／Bootstrap 的 `#ccc`，其餘內建主題恢復為對應色系的凍結分隔線顏色；線寬仍統一為單一 1px。
- Dark Hive 凍結分隔線由 `#666` 提亮為主題格線色 `#aaa`，在深色 header、資料列與 footer 上維持清楚可辨識的 1px 實線。
- 依使用者指示執行正式 build，重建 FabUI 六個主檔、16 組主題資產與 Vue 2／jQuery wrappers；59 項單元測試、完整 smoke、Dark Hive `#aaa` 發布 CSS 與 `git diff --check` 全數通過。
- Row number pane 改為透明且移除整欄右框線，底色與邊界只由實際 `.fg-row-header-cell` 繪製；最後一筆資料列下方不再顯示列號欄底色或直線，Dark Hive／Black 主題同步移除 pane 背景覆寫。
- 依使用者指示重新執行正式 build，將 row number 空白區透明化納入 FabUI 六個主檔與所有主題輸出；59 項單元測試、完整 smoke、dist CSS 規則與 `git diff --check` 全數通過。
- 新增核心 `filterChanged` 事件，`setFilter()`、全域搜尋、欄位搜尋、搜尋運算子、搜尋列顯示切換與所有清除 filter 操作完成後皆會觸發；事件包含來源、條件、active／cleared、remote 與目前 view，並同步 Vue 2 `filter-changed`、jQuery `filterchanged.fabgrid`、API 文件及測試。
- FabGrid constructor options 新增 `updatedView(grid, eventArgs)` callback 綁定，初次 render 與後續 view 更新皆會呼叫；既有 `updatedView.addHandler()` 與 `grid.on('updatedView')` 保持不變。
- 依使用者指示執行正式 build，將 `filterChanged`、Vue 2／jQuery 事件對應與 constructor `updatedView` callback 納入所有發布 bundle；64 項單元測試、完整 smoke、六個主檔及發布檔內容檢查全數通過。
