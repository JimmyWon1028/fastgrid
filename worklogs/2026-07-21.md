# 2026-07-21

## 完成進度

- 完成 `build all`：依序重建 FabUI core、Lite、獨立 Diagram、Gantt、Scheduler 與 19 組 theme／必要圖片，確認所有 build 都沒有 ESM 發佈檔。完整單元測試 482/482 通過，FabUI core、Gantt、Scheduler browser smoke 全數通過。驗證期間修正 Lite build 會把 Mono pagination SVG 額外複製到 `dist/theme/mono/images/` 的問題，現在三組 Mono theme 只共用扁平的 `dist/theme/mono/`；同步將 Scheduler smoke 的 theme metadata gate 更新為 19 組。
- 完成 `build lite min`：以目前 source 重建 FabUI Lite，包含獨立主題 CSS 檔名自動套用、remote filter operator／`in` 字串等最新 FabGrid 行為；最終只保留 `dist/fabui.lite.min.js`、`dist/fabui.lite.min.css` 與必要圖片，確認沒有 Lite 非壓縮檔或 ESM 發佈檔。
- FabGrid 新增獨立主題 CSS 檔名自動辨識：頁面在建立 Grid 前載入唯一的 `fabui.<theme>.css`／`.min.css` 時，Grid root 會自動套用對應 `fg-theme-<theme>`；支援 query string／hash，並排除一般 FabUI／Lite 整合 CSS 與未知 theme。本批未重建 `dist`。
- FabGrid 新增 constructor `filterRules` 初始化 option，接受 `{ field, op, value }[]` 或 JSON 字串；有效規則會在第一次本機 view／遠端 request 前建立 Search Row 篩選，自動顯示 Search Row、填入 input 與 operator 符號，無須建立後逐欄呼叫 setter。本批未重建 `dist`。
- 修正遠端初始化規則的 POST 傳遞：`filterRules` 會保留在 `application/x-www-form-urlencoded` Form Data，未對應顯示欄位的伺服器規則也不再被丟棄；新增 POST body 回歸測試。本批未重建 `dist`。
- `remote: true` 的初始化 `filterRules` 改為保留使用者自訂 `op` 原字串，不再套用本機 operator 白名單；例如 `!%..%` 與 `<>` 會直接送至後端。本機模式維持既有驗證，空字串 value 仍忽略。本批未重建 `dist`。
- FabGrid `select(row, col?)` 增加單參數用法：省略 `col` 時選取第一個可見欄；單參數與雙參數選取若目標列未完整顯示，都會自動捲動並盡量將該列對齊 Grid 可視區第一列，且不改成 `selectRow` 別名。本批未重建 `dist`。
- FabGrid 新增 `unselectRow(row)`：`multiSelectRows: true` 時可明確取消勾選指定列，若該列本來未勾選則不做任何修改，避免 `toggleRowSelection()` 造成反向勾選。本批未重建 `dist`。
- FabGrid 新增 runtime `setFilterRules(rules)`：一次取代舊規則、同步 Search Row input 與運算符，並只觸發一次篩選更新；`remote: true` 保留自訂 `op` 並重新載入一次。傳入空陣列可清除 runtime rules 與 Search Row 值。本批未重建 `dist`。
- FabGrid 新增 `getFilterRules()`：回傳目前實際生效、下次會送給後端的 rules 陣列，包含 Search Row 最新 input 值與 server-only rules；回傳深層副本，外部修改不會影響 Grid。本批未重建 `dist`。
- 同步 Filter Rules runtime API 契約到 `AGENTS.md` 與 `README.md`：`setFilterRules()` 會自動刷新，遠端模式回到第 1 頁並只重新 request 一次；`getFilterRules()` 無有效規則時回傳 `[]`。已完成 `build lite min`，最終僅保留 `dist/fabui.lite.min.js` 與 `dist/fabui.lite.min.css`，並確認沒有 ESM 或非壓縮 Lite 輸出。
- FabGrid Column 未指定 `minWidth` 時的預設下限由 `48px` 改為 `20px`，並同步套用於初始欄寬、拖曳調寬與 AutoFit；個別 Column 明確設定的 `minWidth` 維持優先。本批未重建 `dist`。
- FabGrid 新增全域 `columnMinWidth` option，預設 `20px`；Column 未指定 `minWidth` 時會繼承該值，個別 Column 仍可覆寫，並同步影響初始欄寬、拖曳調寬與 AutoFit。本批未重建 `dist`。
- FabGrid `remote: true` 的 SQL-like filter `op` 新增 Search Row icon 相容映射：`%..%`、`..%`、`%..` 分別顯示包含、開頭、結尾 icon，前置 `!` 顯示對應否定 icon；這個映射只影響 UI，`getFilterRules()` 與 request 仍保留原始 `op`。本批未重建 `dist`。
- FabGrid `remote: true` 的 `op: "in"` 改為以逗號分隔字串送出 `value`；例如 `value: ["ZU001", "AV001"]` 會正規化為 `value: "ZU001,AV001"`。同步套用於 Excel-like filter、constructor／`setFilterRules()`、`getFilterRules()` 與 GET／POST request，Excel-like filter 內部仍保留陣列選取狀態。本批未重建 `dist`。
- 新增第 18、19 組公開 theme：`mono-red` 使用 Metro Red 配色、`mono-green` 使用 Metro Green 配色，兩者與既有 `mono` 共用 `src/theme/mono/images/` 的單色 SVG icon。同步新增 Grid／Tabs／共用控件配色、19-theme metadata、所有 Demo 選項、README／API／AGENTS 契約與回歸測試；本批未重建 `dist`。
- Mono theme 的 `excel.svg` 改用本機 `res/griddy-icons--xls.svg` 圖形，正規化為 `16 × 16` 黑色單色 SVG，以清楚的 `XLS` 字形取代小尺寸下較難辨識的表格輪廓；本批未重建 `dist`。
- Mono theme 的 `datebox_arrow.svg` 改用本機 `res/bi--calendar-date.svg` 圖形，正規化為 `16 × 16` 黑色單色 SVG，讓日期輸入按鈕在小尺寸下保留清楚的日曆與日期數字辨識；本批未重建 `dist`。
- Mono theme 的 `import.svg` 改用本機 `res/mdi--import.svg` 圖形，正規化為 `16 × 16` 黑色單色 SVG，與新版 Export icon 使用一致的實心進出方向語彙；本批未重建 `dist`。
- Mono theme 的 `export.svg` 改用本機 `res/mdi--export.svg` 圖形，正規化為 `16 × 16` 黑色單色 SVG，讓文件出口與向右箭頭在小尺寸下更清楚；本批未重建 `dist`。
- 統一 `fabui.CheckBox`、`fabui.CheckGroup`、`fabui.RadioButton` 與 `fabui.RadioGroup` 的預設 label position 為 `after`，未指定時文字顯示於核取方塊／選項按鈕後方；明確指定 `before`／`top` 的單項元件與 `before` 的群組行為保持不變。本批未重建 `dist`。
- Mono 的 `excel.svg` 已改用本機 `res/icon-park-outline--excel.svg` 圖形，正規化為 `16 × 16` 黑色單色 SVG，並同步重建至 `dist/theme/mono/excel.svg`。
- 修正完整 FabUI build 漏收 `src/editbox/time-editbox.js`，造成 `diagram.html` 載入 `fabui.min.js` 時發生 `createTimeBoxFactory is not defined`：core bundle 清單已補入 TimeBox factory，並新增 build scope 回歸檢查。
- FabGrid 新增共用 `time` cell editor：`editor: 'time'`、相容別名 `timebox` 與 `dataType: 'time'` 都會套用既有 `editorDefinitions.time`；預設 `99:99`、`autoUnmask: true`，支援 `99:99:99`、24 小時驗證、Search Row 數字遮罩、格式化顯示、複製與依游標時／分／秒段落調整的可選 Spinner。三語系驗證文字、README、API、AGENTS 與回歸測試已同步。本批未重建 `dist`。
- Time EditBox 的輸入文字預設對齊方式由置中改為靠左，並補上樣式回歸測試；本批未重建 `dist`。
- `fabui.EditBox` 新增 `editor: 'time'`：預設 `mask: '99:99'`、`autoUnmask: true`、`spinner: false`，另支援 `99:99:99`；只接受數字，分秒限制 `00`–`59`，只有完整 `24:00`／`24:00:00` 可作為上限，無效內容以原生 validity 標記。Time Spinner 沿用 28px 左右位置與三語系文字，並依游標所在時／分／秒段落增減；開發 EditBox Demo、API、README 與測試已同步。本批未重建 `dist`。
- 修正 `npm run build` 原本會整個刪除 `dist/` 的範圍錯誤：FabUI core build 現在只清除並重建 `fabui.*` 與 `dist/theme/`，保留未指定的 Diagram、Lite、Gantt、Scheduler 與其他獨立輸出，符合逗號多範圍 build 不影響清單外 bundle 的契約。
- 擴充 Codex build 命令定義：`build` 新增明確的 `fabui` scope，並支援逗號左右無空白的多範圍格式，例如 `build fabui,diagram min` 會依序編譯兩組並對全部套用 `min`。可組合 `fabui`、`lite`、`diagram`、`gantt`、`scheduler`、`theme`；重複 scope 只執行第一次，`all`／`clear` 必須單獨使用。既有單範圍命令保持相容。本批未執行 build。
- 依 `demo/dev-diagram.html` 再次同步 `demo/diagram.html` 的 Diagram 正式版頁面，保留相同畫面結構、控制項、狀態文字與共用 `diagram-demo.js`，並將獨立 `dist/diagram.css`／`diagram.min.js` cache version 更新到多選屬性版本；正式版仍只載入 `dist/fabui.*`、`dist/diagram.*` 與 `dist-controls.js`。
- FabGrid number cell editor 套用 EditBox 共用 `spinner` definition：支援右側 `true`／`'right'`、左側 `'left'`、`increment`、28px trigger 視覺、min／max、precision、三語系 ARIA 與上下方向鍵增減；未啟用時維持原本跨列移動，Search Row 維持既有導覽。開發 Grid 的應付金額與分數欄分別加入右側與左側範例。本批未重建 `dist`。

- Diagram 同時選取兩個以上圖形時，屬性面板不再只顯示選取數量，改為提供文字大小、粗體、斜體、底線、刪除線、寬度、高度、填滿與框線的批次調整；不同值以 `—` 混合狀態呈現，修改後一次套用全部選取圖形並保留 undo／redo。文字、超連結與 X／Y 不批次覆寫。本批未重建 `dist`。
- Diagram 屬性面板與紙張設定內所有 `editor: 'number'` 的 EditBox 統一啟用 `spinner: true`；目前涵蓋吸附間距、文字大小、X、Y、寬度與高度，並由共用欄位 helper 保證後續新增的 number editor 也預設啟用。本批未重建 `dist`。
- 修正 Diagram 點擊圖形或畫布後偶爾出現瀏覽器原生深色焦點外框：根元素保留 `tabindex`、鍵盤快捷鍵與程式焦點，只停用未受主題控制的預設 `outline`。本批未重建 `dist`。
- 依要求撤回 Diagram 全螢幕浮動檢視工具列的半透明背景，恢復與一般模式相同的不透明工具列；投影片展示／全螢幕 icon 與「符合」文字保持不變。本批未重建 `dist`。
- Diagram 浮動檢視工具列的「投影片展示」與「全螢幕」改為 icon-only FabUI Button：投影片展示採用由 `res/projector-screen.png` 納入 `src/theme/images/` 的正式資產，全螢幕沿用 `icon-fullscreen`；兩者以 tooltip／ARIA 同步進入或離開狀態，右鍵選單文字入口保持不變。「符合內容」三語系文字縮短為「符合／Fit／适合」。本批未重建 `dist`。
- Number EditBox 新增 `spinner` option：`true`／`'right'` 在右側、`'left'` 在左側顯示上下箭頭，Spinner 使用與其他 EditBox trigger 相同的 28px 寬度、theme 背景、icon 色與 hover 視覺，並支援 `increment`、min／max、precision、鍵盤方向鍵、disabled／readonly、三語系 ARIA 與 change event；`demo/dev-editbox.html` 同步加入三種設定範例。本批未重建 `dist`。
- Diagram 紙張設定由畫布空白處雙擊 modal 改為單擊空白處後直接顯示於屬性面板；紙張尺寸、方向與最小 5px 吸附間距皆重用 `fabui.EditBox` 並於變更後立即套用，既有 JSON、`toolboxStateKey` 記憶、格線與 snap 行為保持不變。本批未重建 `dist`。
- Diagram 的 `toolboxStateKey` 擴充為保存完整做圖進度：紙張、nodes 與 connectors 在新增、修改、移動、縮放、刪除、清除、setData、undo／redo 後寫入 `localStorage`，下一次開啟相同 key 時自動載入；舊版僅含版面狀態的內容仍相容。本批未重建 `dist`。
- Diagram 多選兩個以上圖形時新增整組虛線外框與四角等比例縮放；以群組中心固定，同一倍率同步縮放所有圖形的尺寸、位置、彼此間距及內部 connector 控制點，並沿用 undo／redo 與 pointer cancel 還原。本批未重建 `dist`。
- 參考 `demo/dev-diagram.html` 將 `demo/diagram.html` 的 CSS 排版、主題／語系／同側排列選項與四個 FabUI Demo 按鈕標記同步；正式版仍維持載入 `dist/fabui.*`、`dist/diagram.*` 與 `dist-controls.js`。本批未重建 `dist`。
- Diagram 生產製造流程 Demo 的頂端「生產製造流程」標題改為 28px 並加上底線，其餘展示圖內容保持不變。本批未重建 `dist`。
- Diagram 的 node 與 connector 屬性面板新增文字大小、粗體、斜體、底線與刪除線；格式由 Number EditBox 與 FabUI toggle Button 調整，會同步 SVG／就地編輯器、JSON 與 undo／redo。本批未重建 `dist`。

- Diagram 生產製造流程 Demo 的「客戶訂單 → 開立製令」S 線加入「Email或傳真」文字標籤，沿用線條上方與紙張底色遮罩的新標籤樣式；其他節點與連線內容保持不變。本批未重建 `dist`。
- 將 Diagram connector 文字標籤由直接壓在線條上改為顯示於線條中點上方，並在文字後方插入跟隨目前 `pageColor` 的 SVG 背景遮罩，讓線條不穿過文字並從標籤下方清楚接續；路徑、控制點、JSON 與雙擊編輯保持不變。本批未重建 `dist`。
- 移除 Diagram 上方工具列直線、直角線、弧線與 S 線四顆類型按鈕圖示中的箭頭，只保留線型輪廓；實際建立的 connector 仍維持預設 `arrowDirection: 'end'`，由左向右連接時顯示向右箭頭。本批未重建 `dist`。
- Diagram 上方工具列「連線」左側新增直線、直角線、弧線與 S 線四顆 icon-only FabUI Button；四種連線類型互斥選取且預設直角線，支援三語系 tooltip／ARIA，切換類型不會自行開啟連線模式，既有「連線」按鈕仍獨立控制模式開關。本批未重建 `dist`。
- Diagram 上方工具列在「連線」按鈕右側新增垂直分隔線，將連線工具獨立成第四組，工具箱／屬性／唯讀成為第五組；按鈕順序與功能保持不變。本批未重建 `dist`。
- 將 Diagram 生產製造流程 Demo 的「開立製令、倉庫領料、生產工序、完工入庫」由一般圓角方形改為 DFD Process 圓形原型；四個 node 統一使用 `type: 'dfdProcess'` 與 106×106 等寬高尺寸，並保留原中心位置及既有連線。本批未重建 `dist`。
- Diagram 工具箱 DFD 分類新增三語系「S 弧線」connector 工具；建立 `type: 'sCurve'` 的帶箭頭三次貝茲曲線，選取後可拖曳中心控制點，並沿用 JSON、undo／redo 與既有連線 interaction lifecycle。原本 Data Flow 的 `curved` 二次貝茲弧線維持不變，本批未重建 `dist`。
- 再次對照 `man-process.drawio.png` 後，將 Diagram Demo 除兩條垂直直線外的九條平滑連線改為 `sCurve`；S 弧線會依來源與目標圖形的連接邊建立兩端切線，不沿用舊二次弧線控制點，讓製令、投料、領料、庫存、工序、進度與完工之間的走向貼近參考圖。本批未重建 `dist`。
- 依 `man-process.drawio.png` 修正 Diagram 開發版與正式版 Demo 初始圖：不再把流程改排成彩色三列版面，改以 A4 橫向座標重現參考圖的灰階配置，將客戶訂單、投料資料、扣庫存、進度與完工回寫改用 DFD Data Store，製令／領料／工序／完工使用圓形 DFD Process，並重排 11 條直線與曲線連線。

- 將 FabGrid `activeCellBorder` 預設值由 `2` 改為 `1`，同步 active cell、cell editor、CSS fallback、API／README／AGENTS 契約與回歸測試；未重建 `dist`。
- 統一移除所有 build 指令的 ESM 發佈輸出：core、Lite、Gantt、Scheduler、Vue 2 wrapper 與 jQuery wrapper 只產生 browser global JavaScript／壓縮版及適用的 CSS／壓縮版；同步移除 package `module` metadata、更新 smoke／單元測試、README、API 與 AGENTS 契約。各獨立 build 會清除同套件既有的 `.esm.*` 舊檔，本批未重建工作區 `dist`。
- 修正 Search input 按 `ArrowDown` 進入 Grid 時，active cell／selected row 已捲出畫面卻不會回到可視範圍的問題；即使選取 row／column 沒有改變，也會呼叫 `scrollIntoView()` 確保目前 cell 顯示，既有不移動 selected row、不啟動 editor 與焦點轉入 Grid 的行為保持不變。本批未重建 `dist`。
- 將 Diagram 繪圖區右鍵既有的「匯出 PNG、匯出 SVG、列印、投影片展示」四個功能同步加入上方主工具列，兩處共用原有 API 並保留右鍵選單；補齊三語系與投影片展示／離開展示文字同步。本批未重建 `dist`。
- 將 Diagram 上方工具列由右到左整理為檢視控制、編輯／JSON、輸出／展示三組，組間加入兩條跟隨 theme border 色的垂直分隔線；右鍵選單及各按鈕原有功能保持不變。本批未重建 `dist`。
- 解除 Diagram 工具箱與屬性面板不得同側停靠的限制：兩者可獨立 dock 到左側或右側，也可同時位於同一側；同側時維持工具箱在屬性面板之前，拖曳或 API 停靠其中一個面板不再改變另一面板的停靠側，保存狀態可還原同側配置。本批未重建 `dist`。
- 將 Diagram 上方工具列的「下載／載入」從復原／編輯群組拆出，獨立成第四組工具；主工具列現在使用三條垂直分隔線呈現四組，按鈕順序與原有功能保持不變。本批未重建 `dist`。
- 將 Diagram 右下角檢視工具列改為可從縮放百分比或空白處拖曳，按鈕仍保留原點擊功能；工具列會限制在 Diagram 範圍內，並透過既有 `toolboxStateKey` 保存與還原位置，pointer cancel／dispose 會解除互動並還原狀態。本批未重建 `dist`。
- 修正 Diagram 工具列「載入」按鈕圖示：由錯誤的 `icon-reload` 改用 `icon-import`，將 16×16 `import.png` 納入正式 source theme 資產並在 `src/fabui.icon.css` 統一定義；載入 JSON 功能與文字保持不變。本批未重建 `dist`。
- 將 Diagram 工具列的「下載／載入」整組移到最左側，組內順序、三條垂直分隔線、其餘三組與右鍵功能保持不變。本批未重建 `dist`。
- 新增 Diagram 浮動面板前後層級：工具箱與屬性面板同時浮動時，點擊任一面板的標題、內容、控件或 resize 邊緣，會將該面板與所有 resizer 提到最上層；位置、尺寸、拖曳與 dock 行為保持不變。本批未重建 `dist`。
- 將 Diagram 工具箱與屬性面板的同側停靠改為重用 `fabui.Tabs`：兩者共用單一側欄並只顯示作用中面板，工具箱／屬性標籤固定在下方；最後停靠的面板自動成為作用中 tab，切換選擇會透過 `toolboxStateKey` 保存與還原。分居左右或浮動時仍保留既有獨立面板、拖曳與調整尺寸行為。本批未重建 `dist`。
- 在 Diagram 做圖區空白處雙擊開啟的紙張設定視窗加入重用 `fabui.EditBox` Number editor 的「吸附間距」，最小值為 5px；套用後同步更新格線與 node／connector snap，並由 `getPaper()`／JSON `page.gridSize` 保存及匯入還原。本批未重建 `dist`。
- 將 Diagram 工具箱與屬性面板的同側停靠擴充為 `sameSideDockMode: 'tabs' | 'stacked'`：預設保留底部 Tabs，`stacked` 在同一側欄將工具箱置於上方、屬性置於下方並同時顯示；新增 `getSameSideDockMode()`／`setSameSideDockMode()` 執行期切換，並由 `toolboxStateKey` 保存模式。本批未重建 `dist`。
- 將 Diagram 紙張設定納入既有 `toolboxStateKey`：紙張尺寸、方向、寬高、底色與吸附間距在套用後保存，重新載入時完整還原；載入含 `page` 的 JSON 也會同步更新記憶，舊版沒有 `paper` 的 localStorage 內容仍相容。本批未重建 `dist`。
- 將 Diagram「投影片展示」從上方主工具列移至右下角可拖曳檢視工具列，固定放在「全螢幕」左側；做圖區右鍵入口、三語系文字與展示狀態同步保持不變。本批未重建 `dist`。
- Diagram 拖曳圖形時，若未自訂折點的相連直角線端點只差 `gridSize * 0.75`（限制 5 至 16px）即可成為水平或垂直直線，會自動微調整組選取圖形完成對齊；超出門檻、兩端同時移動或已有控制點的連線保持原狀。本批未重建 `dist`。
- 移除 Diagram 浮動檢視工具列「投影片展示」按鈕的 icon，只保留文字；按鈕位置、展示功能、右鍵選單 icon 與完整編輯器「全螢幕」icon 保持不變。本批未重建 `dist`。
- 將 Diagram 開發版與正式版 Demo 的初始做圖資料改為使用 draw.io `man-process.json` 的生產製造流程：保留 14 個有文字圖形及 11 條有效業務連線，忽略標題容器的自我連線，並因來源沒有座標／尺寸／樣式而在 A4 橫向紙張內重新配置三列 FabUI 流程版面。本批未重建 `dist`。
- Diagram 紙張放大超過 viewport 後，新增從空白處左鍵拖曳平移做圖區，直接更新 `scrollLeft`／`scrollTop` 並使用 grab／grabbing 游標；未超出時保留 marquee，放大狀態可用 `Shift`／`Ctrl`／`Cmd` 拖曳強制框選，pointer cancel 會還原起始捲動位置。本批未重建 `dist`。
- 補齊獨立 Diagram build：新增 `build/build-diagram.cjs`、`npm run build:diagram` 與依序編譯 core／Lite／Diagram／Gantt／Scheduler 的 `npm run build:all`，正式版 Diagram Demo 改為先載入 core 再載入 `diagram.*`；獨立輸出不產生 ESM，也不將 Diagram 併入 `fabui.*` core。
- 新增第 17 組公開 `mono` theme：`src/theme/mono/images/` 提供 72 個單色 SVG icon，元件版面與色調沿用 Metro Gray，但保留既有 16 組 selector 與資產不變。Metro Gray 的 Accordion、Layout、Pagination、Panel／Window、Tabs 與 Tree sprite 已在 Mono 中拆成獨立 SVG，Mono CSS 不引用 Metro Gray 或 Default 的 PNG／GIF。所有 source-mode／build-mode Demo、Pivot／ThemeBuilder／Win7 動態清單、公開 theme metadata、README／API／AGENTS 與回歸測試已同步；完成 `npm run build:all`，產生 core、Lite、獨立 Diagram、Gantt、Scheduler 與 `dist/theme/fabui.mono.*`，並將正式 Mono icon 以同名檔案輸出至獨立 `dist/theme/mono/`，其他主題圖片路徑保持不變。472 項單元測試及瀏覽器 smoke test 全數通過。
