## 完成進度

- 依 EasyUI LinkButton 的 icon-only 契約調整 `demo/dev-grid.html`：右上角匯出 CSV、匯出 Excel、全螢幕三個 `fabui.Button` 固定為 34×34 且只顯示 `iconCls`，可見文字保持空白，語系化 `title`／`aria-label` 與既有功能不變；共用 Demo 補強器會保留明確的 `data-icon-cls`，並以 Control registry 避免重複接管已建立的 Button，本批未重建 `dist`。
- 修正所有 `demo/dev-*.html` 經共用補強器轉成 FabUI Combo EditBox 後的選項文字寬度：不再直接沿用會被下拉 trigger 占用的原生 select 總寬，而是依全部 option 的實際字型寬度，加上 textbox padding、frame border 與 trigger 寬度自動調整；動態增減／翻譯 option 時也會重新量測，避免語系、Theme、Pivot、Tabs 等較長內容被截斷，本批未重建 `dist`。
- 修正 `demo/dev-grid.html` 右上角三個 FabUI Button 的文字消失問題：匯出 CSV、匯出 Excel 與全螢幕按鈕會顯示目前語系文字並保留既有 icon／功能，按鈕寬度改為隨內容自動伸展；同步正式版 `demo/grid.html` 的共用 Demo source，本批未重建 `dist`。
- 修正 `demo/dev-grid.html` Popup Grid 的 Window 放大／還原尺寸同步：Window Resize、Maximize、Restore 與 Open 後會從 Control registry 取得內部 FabGrid，並在即時 frame 與 180ms 動畫完成後重新 `invalidate()`；放大到欄位總寬可容納時會正確移除過期的橫向捲動軸，本批未重建 `dist`。
- 完成 `fabui.Diagram` core：參考 DevExtreme Diagram Overview 的工作區配置，以 pure JavaScript SVG 提供工具箱搜尋、九種圖形、node／connector、拖曳與八方向縮放、格線／snap、連線模式、屬性編輯、zoom／fit／fullscreen、undo／redo、JSON 匯入匯出與 SVG 匯出；內部操作按鈕重用 `fabui.Button`，搜尋與屬性欄位重用 `fabui.EditBox`。
- 新增 Diagram 三語系、16 組 theme、Control registry／dispose、source-mode／build-mode Demo、API 操作手冊、單元測試、公開入口與未來 build wiring；本批未重建 `dist`。
- 擴充 `fabui.Diagram` 工具箱至 23 種一般與流程圖圖形；新增空白畫布拖曳框選、Shift／Ctrl／Cmd 累加多選、群組移動／方向鍵／刪除，以及每個選取圖形六個圓形連接點拖曳建立 connector。Connector 以 `fromPoint`／`toPoint` 保存指定端點，未指定時維持原本自動選邊。
- 修正 Diagram connector 幾何：指定六點與自動端點都依連線方向求圖形輪廓交點，橢圓、菱形與多邊形不再連到外接矩形空白處；connector 線條雙擊會在線條中點開啟共用 `fabui.EditBox`，支援 Enter／失焦提交與 Escape 取消。
- 依 General 工具箱參考補齊 Diagram 一般圖形：新增菱形、愛心、八邊形、向上／向下／上下／左右箭頭，保留既有圓角矩形與雲朵；工具箱改為三欄排列，總圖形數增為 30。
- 依 Flowchart 工具箱參考補齊 Diagram 流程圖圖形：新增多重文件、直接資料、內部儲存、紙帶、手動操作、儲存資料、循序資料、合併、頁內連接點、加總接點與 OR 接點，保留既有 Display；總圖形數增為 41。
- Diagram node 支援雙擊圖形中央直接修改文字；與 connector 共用同一套 `fabui.EditBox` 就地編輯、跨 SVG 重繪雙擊判定、Enter／失焦提交與 Escape 取消 lifecycle。
- Diagram node 文字編輯改為透明多行 `fabui.EditBox` 直接疊在圖形內，開啟時自動全選並保留圖形選取框與控制點；Enter 換行、Ctrl／Command + Enter 或失焦提交。
- Diagram 工具箱的「一般／流程圖」分類標題改由 `fabui.Button` 製作，可各自疊合／展開並保留搜尋與語系切換前的狀態。
- Diagram 工具箱 41 種圖形預覽改為重用核心 SVG shape renderer，統一使用空心輪廓並移除另一套實心 `clip-path` CSS 圖案。
- Diagram 新增可疊合 DFD 工具箱：Entity、Process、Data Store 三種節點使總圖形數增為 44，另提供 Data Flow 工具以 `type: 'curved'` 建立帶箭頭弧線 connector；同步補齊三語系、空心 SVG 預覽、JSON 正規化與邊界投影。
- 確認 DFD Entity、Process、Data Store 沿用一般 node 的雙擊圖形內 `fabui.EditBox` 編輯流程，並新增 DFD 節點文字正規化回歸測試與文件契約。
- Panel／Window 的收合、展開、最小化、最大化、還原與關閉工具改用 16 組 theme 對應的 `panel_tools.png` sprite，資產放在 `src/theme/<theme>/images/` 並由 `src/fabui.icon.css` 集中定義；逐一依 `res/themes/<theme>/panel.css`／`window.css` 校正 header、body、footer、frame、gradient、shadow、mask、圓角與 hover 色，正式 source 不依賴 `res/`，本批未重建 `dist`。
- 修正 `fabui.EditBox` Combo popup 寬度：新增預設啟用的 `fitContent` option，Popup 以 EditBox 寬度為下限，選項文字較長時自動加寬，並保留 `panelMaxWidth` 與 viewport 上限；FabGrid 繼續共用同一個 ComboPopup，本批未重建 `dist`。
- 逐一參考 `res/themes/*/layout.css` 修正 `fabui.Layout` 的 16 組 Splitter 固定色、拖曳／focus active 色、collapsed bar 背景、border、文字與字級，移除原本 Material／Metro／深色主題的共用近似色。
- Layout 展開／收合箭頭改用各主題對應的 `layout_arrows.png`，資產複製至 `src/theme/<theme>/images/` 並由 `src/fabui.icon.css` 集中定義；正式 source 不依賴 `res/`，本批未重建 `dist`。
- 移除 Layout 收合／展開 icon 的滑鼠 hover 背景與透明度變化；即使 icon 同時保有焦點，滑鼠位於 icon 上也維持原樣，鍵盤 `focus-visible` 提示只在未 hover 時顯示；同步 README、AGENTS 與 Layout API，本批未重建 `dist`。
- 移除 Layout Splitter 的 hover 亮色；拖曳時只替實際操作的分隔線加上高於遮罩的 active 狀態，其餘分隔線維持原色，鍵盤焦點改用不改背景的虛線提示；本批未重建 `dist`。
- 移除 Window 最小化、最大化、還原、關閉、收合與展開 icon 的滑鼠 hover 背景及透明度變化，保留自訂 tool hover 與鍵盤 `focus-visible` 提示；Panel／Window tool 的狀態色只修改 `background-color`，避免清除 sprite 圖片與座標；同步 README、AGENTS 與 Window API，本批未重建 `dist`。
- 移除 Panel 收合與展開 icon 的滑鼠 hover 背景及透明度變化，保留其他 Panel tool hover 與鍵盤 `focus-visible` 提示；同步 README、AGENTS 與 Panel API，本批未重建 `dist`。
- 將 Window 疊合高度由原本至少 48px 調整為約 38px：疊合狀態使用 26px header 加上 frame padding／border，展開時恢復原設定高度；同步 README、AGENTS 與 Window API，本批未重建 `dist`。
- 調整 `demo/dev-window.html` 初始化：開發版頁面載入完成後直接開啟並置中基本 Window，行為與「置中」按鈕相同；共用 Demo script 與 build-mode `window.html` 不變，本批未重建 `dist`。
- 將 `demo/dev-window.html` 畫面控制項改由 FabUI renderer 接管：主題使用 `fabui.EditBox({ editor: 'combo' })`，工具列與 Modal 關閉操作使用 `fabui.Button`；移除 Demo 對原生 select／button 的 padding 覆寫，並以 mount option 保持 build-mode `window.html` 原行為，本批未重建 `dist`。
- 修正 `fabui.Window` Modal overlay 透明問題：mask 是 Window 的 sibling，原先取不到只定義於 Window root 的 theme variable；現在 16 組 `.fui-window-mask.fg-theme-*` 直接設定各自主題遮罩色，維持既有 z-index 與 inline mask 行為，本批未重建 `dist`。
- 重做 `fabui.Window` 最小化：不再隱藏 Window，而是動畫縮成約 220×38px 並停靠 viewport／inline bounds 左下角；最小化工具切換為還原，點擊後恢復原位置、尺寸與最大化／疊合狀態，viewport resize listener 僅於最小化期間存在；同步 README、AGENTS 與 Window API，本批未重建 `dist`。

- 完成 `fabui.SwitchButton` core：依 EasyUI 1.11.5 的 API、滑動尺寸與 reversed 行為實作，直接保留原生 checkbox 的 name/value、FormData、label、鍵盤、disabled 與 reset 語意，提供 ON／OFF／handle text、readonly、runtime API、三語系、Control registry 與 dispose 還原。
- 逐一參考 `res/themes/*/switchbutton.css` 補齊 16 組主題的 border、ON／OFF／handle 顏色、圓角與字級；正式 source 不依賴 `res/`。
- 新增 SwitchButton source-mode／build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口與未來 build／smoke wiring；本批未重建 `dist`。

- 修正 `fabui.Tabs` overflow 平滑捲動期間的按鈕狀態同步：viewport 捲動時即時更新上一組／下一組按鈕的 disabled 狀態，並沿用 Control managed listener lifecycle。
- 補齊 `fabui.Tabs.dispose()` 作為 `destroy()` 的公開相容方法。
- 修正 `fabui.Tabs.select()` 切換頁籤時漏發 native `unselect` 事件，讓 callback 與 emitter API 行為一致。
- 修正 `fabui.Tabs.add({ id })` 的 tab／panel ARIA 關聯，確保 `aria-controls` 始終指向目前 panel id。
- 調整 `fabui.Tabs` 拖曳觸發範圍：`draggable: false` 時整個 Tab 使用一般箭頭游標；啟用後只有標題文字設為 draggable 並顯示 grab 游標，icon、Tab 留白、tools 與關閉按鈕不再觸發拖曳。
- 完成 `fabui.PropertyGrid` core：參考 EasyUI PropertyGrid／Default theme，提供群組與自訂 group formatter、兩欄設定與排序、共用 EditBox inline editor、boolean editor、變更追蹤、遠端 loader、鍵盤、ARIA、三語系、16 組 theme、Control registry 與 dispose 還原。
- 新增 PropertyGrid source-mode／build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口與未來 build wiring；本批未重建 `dist`。
- 完成 `fabui.CheckBox` core：參考 EasyUI CheckBox／Default theme，保留原生 checkbox 的表單、label、鍵盤與 reset 語意，提供尺寸、checked、disabled、label position／align、runtime API、三語系 ARIA、16 組 theme、Control registry 與 dispose 還原。
- 新增 CheckBox source-mode／build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口與未來 build／smoke wiring；本批未重建 `dist`。
- 完成 `fabui.FileBox` core：參考 EasyUI FileBox／Default theme，直接組合既有 EditBox 與原生 file input，保留 FileList、表單上傳／reset、accept／multiple／capture／required、瀏覽器安全限制、三語系 ARIA、16 組 theme、Control registry 與 dispose 還原。
- 新增 FileBox source-mode／build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口與未來 build／smoke wiring；本批未重建 `dist`。
- 完成 `fabui.CheckGroup` core：參考 EasyUI CheckGroup／Default theme，每個項目直接重用既有 CheckBox 與原生 checkbox input，提供 data／name／value、水平／垂直排列、itemStyle、label、整組／單項 disabled、FormData／reset、三語系、16 組 theme、Control registry 與 dispose 還原。
- 新增 CheckGroup source-mode／build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口與未來 build／smoke wiring；本批未重建 `dist`。
- 補齊今日元件的 16 組 theme：Tabs 保留既有獨立 theme／icon 資源並依 `res/themes/metro*/tabs.css` 修正 Metro 六組為 0px 方角；CheckBox 依 `res/themes/*/checkbox.css` 補上各 theme 主色、圓角與勾選狀態；PropertyGrid 依 `res/themes/*/propertygrid.css` 補上群組、邊框、hover／selected 文字色；FileBox 與 CheckGroup 分別沿用 EditBox／Button 與 CheckBox 的共用 theme 變數，正式 source 與 Demo 不依賴 `res/`，本批未重建 `dist`。
- 暫緩 Vue 2、FabGrid jQuery 與 EditBox jQuery wrapper 開發；預設 `npm run build`／`npm run smoke` 改為只處理 FabUI core 與 theme，不再編譯或強制驗證 `dist/wrapper`，既有 wrapper 原始碼與獨立 build scripts 保留供未來明確恢復時使用。本批未重建 `dist`。
- 完成 `fabui.Form` core：參考 EasyUI Form，管理原生 form 欄位與既有 FabUI 控件，提供 Ajax／一般 submit、queryParams、upload progress、dirty-only、本機／遠端 load、clear、reset、HTML constraint validation、enable／disable／reset validation、resetDirty、Control registry 與 dispose。
- 新增 Form source-mode／build-mode Demo、API 操作手冊、官方契約單元測試、FabUI 公開入口與未來 build／smoke wiring；本批未重建 `dist`。
- 修正 `fabui.Form` validation 視覺：invalid 狀態只標記實際 textbox shell，不再把 EditBox label 一起框紅；新增使用原生 `validationMessage` 的 EasyUI 風格黃色箭頭提示，支援 focus／hover、外部點擊、Escape、viewport 重定位與開啟期間 listener lifecycle；本批未重建 `dist`。
- 修正 Form validation tip 語系：新增 `locale`／`setLocale()` 與 `en`、`zh-TW`、`zh-CN` 訊息，依 `ValidityState` 產生必填、型別、pattern、長度與範圍錯誤，不再受瀏覽器介面語言影響；`setCustomValidity()` 保留呼叫端原文，本批未重建 `dist`。
- 完成 FabUI 多語系／theme 全元件稽核：所有具核心 UI 文字的公開元件統一檢查 `en`、`zh-TW`、`zh-CN` key，補正 `zh-Hant`／`zh_Hant_TW` 與 `zh-Hans`／`zh_CN` alias；EditBox Date／Combo／Color 新增 runtime `setLocale()` 與完整 trigger／popup 輔助文字更新。
- 補正 theme 缺漏：公開 theme-aware 元件與 Pivot controls 統一公布 16 組 `themes` metadata；Form validation tip、ComboPopup、ColorPopup 會攜帶目前 theme；Chart 改用共用 theme variables 並新增 `theme`／`setTheme()`；PivotWorkspace 新增 `theme`／`setTheme()`，同步 nested Grid／Panel／Chart root，避免子元件退回 default。新增跨元件 locale／theme audit tests，本批未重建 `dist`。
- 修正 `demo/dev-grid.html` Popup Grid 交給 `fabui.Window` 後 pager 被排成右側窄欄的問題：Window body 明確使用直向 flex layout，pager 固定為底部橫列並禁止狀態文字逐字折行；確認問題位於 Demo Window 整合樣式，FabGrid pagination core 不需修改。本批未重建 `dist`。
- 完成 `fabui.RadioButton` core：參考 EasyUI RadioButton，保留原生 radio 的同名互斥、FormData、label、鍵盤、disabled 與 reset，支援 runtime API、三語系、16 組 theme、Control registry 與 dispose 還原。
- 完成 `fabui.RadioGroup` core：參考 EasyUI RadioGroup，每個項目直接重用既有 RadioButton 與原生 radio，提供單一 name／value、data、水平／垂直排列、itemStyle、整組／單項 disabled、FormData／reset、三語系、16 組 theme 與 lifecycle。
- 新增 RadioButton／RadioGroup source-mode 與 build-mode Demo、API 操作手冊、單元測試、FabUI 公開入口、`demo/dev.html`／`demo/index.html` 項目與未來 build wiring；本批未重建 `dist`。
- 將 `fabui.Tree` 的展開／收合、階層線、資料夾、檔案、checkbox 與 loading icon 改為 EasyUI Tree sprite 樣式；16 組 FabUI theme 各自使用 `src/theme/<theme>/images/` 下的對應資產，正式 source 不直接依賴 `res/`，本批未重建 `dist`。
- 修正 `fabui.Tree` 非 Default 主題只換 sprite、未完整對照 Tree 狀態色的問題：逐一依 16 組 `res/themes/<theme>/tree.css` 同步 hover／selected 背景與文字色、editor 邊框及 Bootstrap 字級，Black／Dark Hive 的一般文字不再因白底白字而消失；本批未重建 `dist`。
- 將所有 `demo/dev*.html` 的輔助按鈕、下拉、輸入、checkbox、radio 與 file 欄位統一交由 `demo/js/dev-controls.js` 使用 FabUI 元件補強；保留各頁既有事件、表單與動態 select options，同步主題切換，並排除 Grid／Window／Calendar／Date／Combo／Color popup 等元件內部控制項，避免重複包裝。
- 清理 source-mode Demo 的樣式污染：移除 Pivot、Remote Grid、Tooltip、EditBox、Messager、Grid／TreeGrid 等頁面對原生 button 的框線、背景、padding、hover 與字型覆寫，Demo CSS 僅保留容器排版與必要寬度；新增跨頁稽核測試並以瀏覽器逐頁驗證，本批未重建 `dist`。
- 統一 `fabui.Button` 的 LinkButton host 契約為 `<a>`：核心不再接受 `<button>`，未提供 href 時自動補上無導覽 href；MenuButton、SplitButton、Messager、ThemeBuilder、Grid 工具列、source／build Demo 與 API 範例同步改用 anchor，保留 disabled、Space／Enter、Form submit 與 dispose 還原語意。新增跨頁 host 稽核，377 項測試及 Button／MenuButton／Form／Grid 瀏覽器驗證通過，本批未重建 `dist`。
- 修正 PivotWorkspace 點擊 Chart 後移動滑鼠時的 Tooltip 例外：公開 `chart.tooltip` option 與內部 Tooltip DOM 原本共用同一 property，現在 DOM 改由 `tooltipElement` 管理，保留既有公開 option API，並在 hide／dispose lifecycle 加入安全處理；新增回歸測試並完成 source-mode 圖表點擊驗證，本批未重建 `dist`。
- 完整移除 FabUI EditBox jQuery wrapper：刪除 `packages/fabeditbox-jquery` 原始碼／型別／輸出、獨立 build script、source／build Demo、API 文件與專用測試，清除 package scripts 與入口連結，並新增移除狀態防回歸測試；FabUI core 的 `fabui.EditBox` 與 FabGrid jQuery wrapper 不受影響，本批未重建 `dist`。
- 將非 `dev*.html` 的正式 Demo 工具列、表單欄位與操作按鈕也統一交由 FabUI 元件接管：抽出 `demo/js/demo-controls.js` 共用補強器，開發版注入 `src/`、正式版注入 `dist/`，正式頁面的 Button host 全部改為 anchor，並保留原事件、表單 submit 與動態選項行為。Demo 索引統一為 `demo/index.html`，移除 `demo/dev.html` 與「開發版」索引欄位；Vue Demo 依既有暫緩範圍不處理，本批未重建 `dist`。
- 依影片調整 `fabui.Layout` Splitter 拖曳：pointer move 期間只移動亮色分隔線預覽，不再持續更新 region option、Panel 尺寸或觸發內容重排；pointer up 時才一次提交尺寸並發出 `RegionResize`，pointer cancel 則還原預覽且不改變 Panel，避免 Grid 等內容在拖曳過程反覆重繪。本批未重建 `dist`。
- 依使用者撤回要求恢復 `demo/dev.html` source-mode 開發版索引，並讓 `demo/index.html` 再次並列開發版與正式版連結；保留已移除的 EditBox jQuery wrapper 與暫緩的 Vue／jQuery wrapper build 範圍。
- 依後續確認將索引分工調整為：`demo/dev.html` 以一般文字連結並列開發版與正式版，`demo/index.html` 只顯示正式版；索引連結不套用 `fabui.Button`。
- 再依使用者釐清，將 `demo/dev.html` 與 `demo/index.html` 定義為純 HTML 索引，移除 FabUI CSS、JavaScript 與共用 Demo 控件補強器；連結只保留原生 `<a href="...">文字</a>`。
- 依參考影片調整 `fabui.Window` 八方向縮放：pointer move 期間只更新跟隨滑鼠的虛線 resize proxy，不再修改實際 Window geometry、options 或內容尺寸；pointer up 才一次套用新位置／尺寸並觸發 `Resize`，pointer cancel 則移除 proxy 並保留原尺寸，避免內嵌元件持續 render。本批未重建 `dist`。
- Diagram 畫布空白處雙擊新增 modal 紙張設定：重用 `fabui.Window`、`fabui.EditBox` 與 `fabui.Button`，預設 A4 橫向，支援 A3／A4／A5／Letter／Legal 及直向／橫向，設定同步保存於 Diagram JSON `page`；圖形與線條雙擊仍維持就地文字編輯。已重建 FabUI core、ESM、CSS、16 組 theme 與圖片資產，Smoke 驗證及 394 項單元測試全部通過，Vue、FabGrid jQuery 與 EditBox jQuery wrapper 未納入 build。
- 修正 Layout 內建收合與疊合後展開工具在正式 CSS bundle 中不可見：Panel 基礎 tool 與 Layout expand button 都改為只設定透明背景色，不再以 `background` shorthand 清除 `layout_arrows.png` sprite；新增回歸測試確保收合／展開 icon 不會被覆蓋，並已重建 FabUI core 與 theme `dist`。
- 完整操作 `demo/layout.html` 的四向收合／展開、四條 Splitter、East 動態移除／加入與 16 組主題，確認箭頭 sprite、方向及 Panel 狀態正確；補上工具列遺漏的 North 切換展示，並讓正式版與開發版共用同一套四向切換邏輯與回歸測試。已重建 FabUI core、16 組 theme 與圖片資產，395 項單元測試及正式 `dist` smoke 全部通過。
- 逐頁開啟並比對 `demo/dev.html` 的 30 組開發版／正式版 Demo，而非只比索引：補齊正式版 FabGrid icon-only 工具列、Tabs 拖曳開關、Window 預設置中視窗、TreeGrid 搜尋列／列號控制、PivotGrid 主題／語系／總計／完整 PivotChart、PivotWorkspace 欄位與 Pie 預設，並同步 Tree、表單元件、Menu 系列與 Grid 組合頁的展示文字及共用 script 版本。新增逐頁展示簽章測試，確保每組控制項 ID、input 狀態、select 選項與展示段落保持一對一；已重建 FabUI core、CSS、16 組 theme 與圖片資產，396 項單元測試、瀏覽器逐頁／主要操作驗證及正式 `dist` smoke 全部通過。
